{"ok":true,"doc_id":"flowgate.default.0492.0010-L","type":"L","title":"API 프로바이더 server-mediated 도구 디스패치 로직설계","status":"open","revision_no":1,"owner_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","triggered_by":"flowgate.default.0492.0001-B","group_id":"flowgate.default.0492","project":"flowgate","module":"default","branch":"main","stored_path":"documents/flowgate/main/default/0492/0010-L_document.md","content":"# API 프로바이더 server-mediated 도구 디스패치 로직설계 (L)\n\n> 단계: 로직설계(L) — WP0004 L#3\n> 참조: D0005, P0006, P0007, L0008, L0009, NR0003 R4·R5·R6\n\n---\n\n## 목적\n\nserver-mediated `register_document`가 모델이 제출한 project/group/doc/action을 신뢰하지 않고 run과 token의 서버 컨텍스트에서 등록 요청을 조립하는 절차를 확정한다. 또한 inbox 등록 경계의 모든 context-binding 403이 project/group/doc/action 중 어긋난 축을 기계적으로 식별할 수 있도록, 외부 오류와 내부 진단을 일관되게 남기는 절차를 확정한다.\n\n## 1. 파라미터 정의\n\n| 파라미터 | 값 | 단위 | 설명 | 출처 |\n|---|---:|---|---|---|\n| `binding_axes` | `action`, `project`, `group`, `doc` | 축 4개 | register 요청의 서버 고정 바인딩 축 | NR0003 R4·R5·R6 |\n| `model_register_fields` | `title`, `content` 또는 `doc_path`, `doc_type`, scope별 업무 필드 | 필드 집합 | 모델이 결정할 수 있는 문서 내용 영역 | P0007, L0009 |\n| `server_register_fields` | `action`, `project`, `module`, `group_name`, `prev_doc_id` 또는 `doc_id` | 필드 집합 | 모델 입력을 무시하고 run/token에서 조립할 영역 | NR0003 R4 |\n| `binding_error_status` | 403 | HTTP 상태 | 인증 성공 후 서버 컨텍스트가 불일치할 때의 응답 | 현행 inbox 계약 |\n| `binding_error_code` | `forbidden` | 코드 1개 | P0007 공통 오류 정규화에 따른 모든 HTTP 403의 top-level tool_result 코드 | P0007 |\n| `binding_axis_order` | `action`, `project`, `group`, `doc` | 순서 4개 | 복수 불일치 시 대표 축과 진단 배열의 고정 순서 | 설계 결정 |\n| `diagnostic_value_policy` | expected/actual의 원문 미기록 | 정책 1개 | token·문서 식별자 누출 억제 | 보안 경계 |\n\n수치 계산과 나눗셈은 없다. `binding_axis_order`는 오류 재현성과 테스트 안정성을 위한 단일 진실 공급원이다.\n\n## 2. 알고리즘 / 처리 로직\n\n### 2.1 R4 — register 도구 입력 표면 분리\n\nprovider에 공개하는 `register_document` schema에는 `action`, `project`, `module`, `group`, `group_name`, `doc_id`, `doc_ref`, `target_id`, `prev_doc_id`를 넣지 않는다. 이 필드가 모델 응답에 추가로 나타나면 schema의 `additionalProperties=false`에 따라 `schema_validation_failed`로 거부하며 inbox까지 전달하지 않는다.\n\n모델은 문서의 내용과 scope별 업무 값만 제출한다. 서버는 provider 호출 전에 확정해 둔 run을 다시 읽고, 현재 bearer token을 검증한 뒤 다음처럼 envelope을 조립한다.\n\n```text\nfunction assemble_register_request(run_id, raw_token, model_input):\n    run = run_store.get_for_update(run_id)\n    token = token_service.verify(raw_token)\n    context = bind_registration_context(run, token)\n    payload = validate_model_document_fields(model_input, context.action)\n\n    envelope = payload\n    envelope.action = context.action\n    envelope.project = context.project\n    envelope.module = context.module\n    envelope.group_name = context.group\n\n    if context.action == new:\n        envelope.prev_doc_id = context.doc\n    elif context.action is edit or review or test_run:\n        envelope.doc_id = context.doc\n    else:\n        return invalid_scope\n\n    remove every client-supplied alias of server_register_fields\n    return immutable envelope\n```\n\n`doc`은 new에서는 선행/현재 workflow 문서, edit·review·test_run에서는 대상 문서를 뜻한다. continuation token으로 홉이 바뀌면 서버가 run의 current token과 네 축을 원자적으로 갱신한 뒤에만 다음 register 호출을 허용한다. 이전 turn에서 만든 지연 호출은 token owner 또는 축 불일치로 거부한다.\n\n### 2.2 R5 — run/token 컨텍스트 결합\n\n```text\nfunction bind_registration_context(run, token):\n    require run exists and is active\n    require token exists, is active, unexpired, and unconsumed\n    require token.token_id == run.current_token_id\n\n    run_context = canonicalize(\n        action = run.action_scope,\n        project = run.project_id,\n        group = run.group_id,\n        doc = run.doc_ref\n    )\n    token_context = canonicalize(\n        action = token.action_scope,\n        project = token.project,\n        group = token.group or group_of(token.doc_ref),\n        doc = token.doc_ref\n    )\n\n    mismatches = compare_binding_axes(run_context, token_context)\n    if mismatches is not empty:\n        record_binding_failure(boundary=register_dispatch, mismatches, run, token)\n        return forbidden(reason=context_binding_mismatch)\n\n    require group_of(run_context.doc) == run_context.group\n    require project_of(run_context.group) == run_context.project\n    return run_context plus server-owned module\n```\n\ncanonicalize는 별칭을 허용하지 않고 저장소의 정규 project id, 완전한 group id, 완전한 document id, 소문자 action enum으로 변환한다. token에 group 열이 없는 이전 레코드는 `doc_ref`의 DB 소속 group을 조회해 보완하되, 조회 실패 시 group 축 불일치로 fail-closed한다. request body는 이 비교에 참여하지 않는다.\n\n등록 dispatcher는 조립한 envelope을 inbox 내부 함수 또는 동일 서버의 inbox endpoint에 전달한다. 어느 방식을 쓰든 bearer token은 현재 token 하나만 사용하고, 모델 대화·도구 인자·prompt에서 얻은 token이나 바인딩 값은 전달하지 않는다.\n\n### 2.3 R6 — inbox 수신 경계 재검증\n\ndispatcher에서 결합을 마쳤더라도 inbox는 독립적으로 네 축을 다시 검증한다. 현행 new/edit/review/test_run의 반복된 조건문은 공통 판정기로 모으고, group 검사를 명시적으로 추가한다.\n\n```text\nfunction validate_inbox_binding(action_handler, request_context, token, token_kind):\n    expected = canonicalize(\n        action = action_handler,\n        project = request_context.project,\n        group = request_context.group,\n        doc = request_context.prev_doc_id when action_handler == new\n              else request_context.doc_id\n    )\n    actual = canonicalize(\n        action = token.action_scope,\n        project = token.project,\n        group = token.group or group_of(token.doc_ref),\n        doc = token.doc_ref\n    )\n    mismatches = compare_binding_axes(expected, actual)\n\n    if action_handler == review and token_kind == human_legacy:\n        require request_context is not associated with an api_provider_run\n        if token.doc_ref is null or empty:\n            remove doc from mismatches\n            record audit event with binding_relaxed=true and relaxed_axis=doc\n\n    if mismatches is not empty:\n        record_binding_failure(boundary=inbox, mismatches, request_context, token)\n        return 403 response for mismatches\n    return pass\n```\n\nnew의 group은 정규화된 `group_name`, edit/review/test_run의 group은 대상 문서의 DB 소속 group을 사용한다. doc id 문자열에서 group을 추측한 값만으로 통과시키지 않는다. 대상 문서 조회가 필요한 경로는 존재 확인 전 식별자 노출을 막기 위해 token의 doc_ref로 안전하게 조회하고, 없으면 doc 축 불일치로 기록한다. binding 통과 뒤 일반 referential-integrity 단계에서 요청 대상의 404/422를 판정한다.\n\n### 2.4 403별 식별 가능한 오류 표면\n\n```text\nfunction compare_binding_axes(expected, actual):\n    result = []\n    for axis in binding_axis_order:\n        if expected[axis] != actual[axis]:\n            append axis to result\n    return result\n\nfunction forbidden_binding_response(mismatches, correlation_id):\n    return status 403 with {\n        ok: false,\n        error: {\n            code: forbidden,\n            message: \"Context binding mismatch. Use the correct token.\",\n            details: {\n                reason: context_binding_mismatch,\n                axis: first mismatch in binding_axis_order,\n                axes: mismatches,\n                correlation_id: correlation_id\n            }\n        }\n    }\n```\n\nP0007의 공통 tool_result 정규화 계약을 우선 적용한다. 모든 HTTP 403은 원인이 무엇이든 top-level `error.code=\"forbidden\"`이며, `context_binding_mismatch`와 `permission_denied`는 새 top-level code가 아니라 각각 `error.details.reason` 값이다. context-binding 403은 추가로 `details.axis`와 `details.axes`를 남기고, 권한 부족 403은 `details.reason=\"permission_denied\"`만 남겨 binding 실패와 섞지 않는다. 응답에는 expected/actual 값, raw token, token id, scratch path를 넣지 않는다.\n\n서버 구조화 로그와 run 진단에는 다음 키를 남긴다.\n\n| 키 | 값 |\n|---|---|\n| `event` | `inbox_context_binding_rejected` |\n| `boundary` | `register_dispatch` 또는 `inbox` |\n| `axis`, `axes` | 고정 순서의 대표 축과 전체 불일치 축 |\n| `action_handler` | 진입한 handler의 서버 상수 |\n| `run_id`, `correlation_id` | 추적 식별자 |\n| `token_id_hash` | 원문이 아닌 비가역 식별값 |\n| `expected_fingerprint`, `actual_fingerprint` | 네 축을 서버 secret으로 HMAC한 값 |\n\n각 기존 403 반환 지점은 공통 helper 호출로 교체하여 source 위치와 축을 대응시킨다.\n\n| handler | 기존 비교 | 기록 축 |\n|---|---|---|\n| `new` | token.project ↔ request.project | `project` |\n| `new` | token.action_scope ↔ `new` | `action` |\n| `new` | token.doc_ref ↔ request.prev_doc_id | `doc` |\n| `new` | token/group DB 소속 ↔ request.group_name | `group` |\n| `edit` | token.project ↔ request.project | `project` |\n| `edit` | token.action_scope ↔ `edit` | `action` |\n| `edit` | token.doc_ref ↔ request.doc_id | `doc` |\n| `edit` | token/group DB 소속 ↔ target document group | `group` |\n| `review` | token.action_scope ↔ `review` | `action` |\n| `review` | token.project ↔ request.project | `project` |\n| `review` | bound token.doc_ref ↔ request.doc_id | `doc` |\n| `review` | token/group DB 소속 ↔ target document group | `group` |\n| `test_run` | token.action_scope ↔ `test_run` | `action` |\n| `test_run` | token.project ↔ request.project | `project` |\n| `test_run` | token.doc_ref ↔ request.doc_id | `doc` |\n| `test_run` | token/group DB 소속 ↔ target document group | `group` |\n\nreview의 현행 `doc_ref in (None, \"\", doc_id)` 완화는 위 의사코드처럼 `token_kind == human_legacy`이고 API provider run과 무관한 경우에만 적용한다. 이때 actual을 위조하거나 expected doc을 대입하지 않고, 비교 후 빈 doc_ref로 생긴 `doc` mismatch만 제거하며 `binding_relaxed=true`, `relaxed_axis=doc` 감사 로그를 남긴다. run-bound review token을 포함한 그 밖의 token kind는 doc_ref가 정확히 일치해야 하며, `human_legacy` token은 API provider run에 진입시킬 수 없다.\n\n### 2.5 완료 처리와 불변식\n\ninbox 성공 응답의 `next_token`과 `next_mention`만 다음 홉 입력으로 인정한다. L0009 §2.7의 완료 도구 실패 계약을 그대로 적용하여, `register_document` 실패는 원인과 무관하게 diagnostics를 기록한 뒤 현재 provider turn을 끝내고 다음 provider turn으로 계속한다. context-binding 403이면 `register_errors`에 `status=403`, `code=forbidden`, `reason=context_binding_mismatch`, `axis`, `axes`, `turn`, `correlation_id`를 기록한다. 권한 부족 403이면 `code=forbidden`, `reason=permission_denied`를 기록한다. 모델에게 서버 소유 바인딩 필드를 고치도록 유도하지 않으며, 이 실패 자체만으로 run을 종료하지 않는다. run 종료는 L0009가 정한 고정 컨텍스트 실패·취소·기한 등 별도 failed 경로에서만 일어난다.\n\n다음 불변식은 모든 register 호출 전후에 성립해야 한다.\n\n```text\nrequest.action  == run.action_scope == token.action_scope\nrequest.project == run.project_id   == token.project\nrequest.group   == run.group_id     == group_of(token.doc_ref)\nrequest.doc     == run.doc_ref      == token.doc_ref\n```\n\n## 3. 상태 전이\n\n| 상태 | 설명 | 진입 조건 |\n|---|---|---|\n| `model_payload_received` | 내용 필드만 수신 | register tool call 파싱 성공 |\n| `context_bound` | run/token 네 축 일치 | R5 판정 통과 |\n| `envelope_assembled` | 서버 필드 주입 완료 | R4 조립 성공 |\n| `inbox_revalidated` | 수신 경계 재검증 완료 | R6 통과 |\n| `registered` | 문서 동작 저장 완료 | 권한·참조·본문 검증 및 저장 성공 |\n| `binding_rejected` | 하나 이상 축 불일치 | dispatcher 또는 inbox 비교 실패 |\n\n```text\n[model_payload_received] --R5 일치--> [context_bound] --R4 조립--> [envelope_assembled]\n[envelope_assembled] --R6 일치--> [inbox_revalidated] --> [registered]\n[model_payload_received|envelope_assembled] --축 불일치--> [binding_rejected]\n```\n\n`binding_rejected`는 같은 모델 turn에서 자동 복구하지 않는다. 실패 diagnostics를 provider에 전달한 뒤 L0009의 turn 루프에 따라 다음 provider turn에서 새 호출을 평가한다. 서버가 token/run 컨텍스트를 교체하는 경우에는 두 값을 원자적으로 결합한 뒤 처음부터 평가하며, register 실패만으로 run 상태를 failed로 전이하지 않는다.\n\n## 4. 결정 트리 / 분기 조건\n\n```text\nif model input contains a server-owned binding field:\n    → 422 schema_validation_failed; inbox 미호출\nelif run or token is absent, inactive, expired, or consumed:\n    → 기존 인증/상태 오류\nelse:\n    mismatches = compare run and token on action/project/group/doc\n    if mismatches is not empty:\n        → 403 forbidden with details.reason=context_binding_mismatch and axis diagnostics; 다음 provider turn으로 계속\n    else:\n        assemble request only from run/token plus allowed model content\n        revalidate request and token at inbox\n        if inbox mismatches is not empty:\n            → 같은 forbidden 형식과 구조화 로그; side effect 없음; 다음 provider turn으로 계속\n        elif permission is insufficient:\n            → 403 forbidden with details.reason=permission_denied; binding 축을 표시하지 않음; 다음 provider turn으로 계속\n        else:\n            → scope별 등록 처리\n```\n\n## 5. 경계 조건 및 예외 처리\n\n| 상황 | 발생 조건 | 처리 방법 | 비고 |\n|---|---|---|---|\n| 복수 축 불일치 | 두 축 이상이 다름 | `axes`에 전부, `axis`에는 고정 순서 첫 축 | 한 요청 한 진단 이벤트 |\n| group 열 없는 legacy token | token에 group 미저장 | doc_ref의 DB 소속으로 보완; 실패하면 `group` 불일치 | 모델 값 사용 금지 |\n| new의 선행 문서가 다른 group | prev_doc_id 소속과 request group 불일치 | `group` 축 403 | 참조 검증 전 차단 |\n| review의 빈 doc_ref | API run token에 doc_ref 없음 | `code=forbidden`, `reason=context_binding_mismatch`, `doc` 축 403 | `human_legacy` 비-provider token만 비교 후 doc mismatch 제거 |\n| continuation 경쟁 | token 교체 중 이전 호출 도착 | owner 또는 네 축 비교 실패로 403 | side effect 없음 |\n| 값 대소문자·별칭 | action/project/group/doc 표기가 다름 | canonical form 외 값은 불일치 | 묵시적 보정 금지 |\n| 문서/그룹 조회 실패 | DB 오류 또는 대상 없음 | fail-closed, `unavailable` 또는 doc/group 축 진단 | 식별자 원문 비노출 |\n| 로그 기록 실패 | 진단 sink 장애 | 403 반환과 등록 차단은 유지, fallback 보안 로그 시도 | 등록 허용 금지 |\n| 외부 호출 타임아웃 | 내부 inbox 호출이 시간 초과 | `unavailable`, 중복 재등록 방지용 idempotency 확인 후만 재시도 | 무한 재시도 없음 |\n| 권한 부족 | binding은 일치하나 RBAC 실패 | `code=forbidden`, `details.reason=permission_denied` 403 후 다음 provider turn | 축 오류로 오분류 금지 |\n\n이 로직은 나눗셈을 사용하지 않으므로 분모 0 조건은 없다. 참조 데이터 부재는 임의 기본값으로 통과시키지 않는다.\n\n## DEFERRED\n\n없음.\n\n## 체크리스트\n\n- [x] 모델 입력과 서버 고정 register 필드를 분리했다.\n- [x] project/group/doc/action 네 축의 run/token 조립 순서를 확정했다.\n- [x] new/edit/review/test_run의 모든 context-binding 403에 식별 가능한 축을 배정했다.\n- [x] P0007의 403 `code=forbidden` 정규화와 details 원인 구분을 적용했다.\n- [x] L0009의 완료 도구 실패 후 다음 provider turn 계속 계약을 적용했다.\n- [x] legacy review 완화를 token kind 분기로 의사코드에 반영했다.\n- [x] 외부 응답과 내부 로그의 민감값 비노출 규칙을 확정했다.\n- [x] 모든 결정 트리에 기본 거부 분기가 있다.\n- [x] 나눗셈이 없음을 경계 조건에 명시했다.\n\n---\n\nnext_type: L\nnext_type_detail: 로직\nproject: flowgate\nmodule: default\ngroup: 0492\ntitle: API 프로바이더 register 컨텍스트 바인딩 구현 로직설계\ntarget_id: B0001\n","doc_review_status":"approved","rejection_reason":"## Automated review rejection\n\nR4/R5/R6의 축 정의, 서버 조립 순서, 진단 로그 정책 등 큰 골격은 D0005/NR0003과 정합합니다. 다만 이 문서가 다루는 register_document 실패 표면이 이미 승인된 P0007(HTTP 403은 항상 code=forbidden, 원인은 details로만 구분)과 L0009(완료 도구 실패는 원인 불문 diagnostics 기록 후 다음 turn으로 계속, run을 종료하지 않음)의 확정 계약과 맞닿아 있는데, 이 문서는 그 두 계약과 반대되거나 반영하지 않은 새 규칙(top-level context_binding_mismatch/permission_denied 코드, register 실패 시 즉시 run 종료)을 도입하면서 P0007/L0009 쪽 개정 필요성을 언급하지 않습니다. 또한 review의 legacy 완화 예외가 서술에만 있고 validate_inbox_binding 의사코드에는 반영되지 않았습니다. 세 지점을 P0007/L0009와 정합하도록 명시하거나, 두 상위 문서의 해당 부분이 함께 바뀌어야 함을 이 문서에서 못박아야 합니다.\n\n- 2.4 403별 식별 가능한 오류 표면 (forbidden_binding_response) 및 2.5 register 실패 처리의 permission_denied: 이미 승인된 P0007의 [공통 오류 코드] 표는 register_document(모든 action_scope)를 포함한 모든 tool_result 실패를 HTTP status 하나만 보고 고정된 enum(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)으로 정규화하며, 403은 항상 code=\"forbidden\"이고 세부 원인은 details로만 구분한다고 명시한다('code는 내부 응답의 HTTP status만 보고 아래 표로 고른다 — 어느 계층·어느 서비스가 잡았는지는 코드에 반영하지 않는다'). 그런데 이 문서(L0010)는 register 경로의 403 top-level error.code를 context_binding_mismatch로, 권한 부족 403은 별도로 permission_denied로 정의한다(2.4 forbidden_binding_response, 2.5). 이는 P0007이 고정한 7개 code 열거값 밖의 새 top-level code를 register_document 실패 표면에 도입하는 것으로, L0009가 정확히 같은 이유(group_worktree_unavailable/invalid_tool_call/toolset_unavailable을 top-level code처럼 쓰다가 반려되어 conflict/invalid_request/unavailable + details.reason으로 고친 전례)로 반려·수정된 패턴을 반복한다. context_binding_mismatch/permission_denied가 실제로 모델에게 가는 register_document tool_result의 top-level code인지, 아니면 P0007처럼 code=\"forbidden\" 아래 details.reason(또는 details.axis/axes와 별개의 필드)으로 들어가는 내부 원인 라벨인지 이 문서가 명시하지 않았다. 명시하지 않으면 L0009/P0007의 tool_result 정규화 계약과 이 문서의 raw 403 계약 중 무엇을 구현해야 하는지 알 수 없다.\n- 2.5 완료 처리와 불변식 — register 실패 시 run 종료: 이 문서는 \"register 실패 시 모델이 바인딩 필드를 고쳐 재시도하도록 유도하지 않고, register_errors에 status=403, reason=context_binding_mismatch, axis, axes, turn, correlation_id를 기록한 뒤 run을 fail-closed 종료한다\"고 규정한다. 그러나 이미 승인된 L0009 §2.7의 turn 루프 의사코드는 완료 도구(register_document) 호출이 실패하면 실패 원인과 무관하게 \"record diagnostics when completion call\" 후 \"break to next provider turn\"으로 처리하여 다음 provider turn으로 루프를 계속하고 run을 종료하지 않는다(run 종료는 오직 고정 컨텍스트 실패/취소/기한 같은 별도 failed 경로에서만 일어난다). context_binding_mismatch만 예외적으로 run을 즉시 종료하려면 L0009가 정의한 turn 루프에 이 분기(어떤 조건에서 diagnostics 기록 후 계속 진행하지 않고 곧바로 run을 종료로 전이시키는지)를 추가해야 하는데, 이 문서는 L0009의 루프를 전혀 참조하거나 개정 필요성을 언급하지 않는다. 두 문서를 그대로 구현하면 어느 쪽 동작이 맞는지 충돌한다.\n- 2.3 R6 — validate_inbox_binding 의사코드 대 review의 legacy 완화 서술: 본문은 \"review의 현행 doc_ref in (None, \\\"\\\", doc_id) 완화는 server-mediated register에 적용하지 않는다 ... 사람용 legacy token 호환이 필요하면 별도 token kind에서만 허용하고 binding_relaxed=true 감사 로그를 남기며 API provider run에는 진입시키지 않는다\"고 하여, review의 doc 축 완화 여부가 token kind에 따라 갈린다고 서술한다. 그러나 바로 위 validate_inbox_binding 의사코드는 token kind를 입력 파라미터로도, 분기 조건으로도 전혀 다루지 않고 canonicalize(action/project/group/doc) 후 compare_binding_axes만 수행한다. legacy token의 완화 예외를 이 의사코드의 어느 지점(actual 계산 전에 완화된 doc 값을 대입하는지, 아니면 compare_binding_axes 이후 doc 축 mismatch를 무시하는 별도 분기를 두는지)에 넣을지가 알고리즘에 없어, 서술한 예외 규칙과 확정한 알고리즘이 서로 어긋난다.\n\nGET http://127.0.0.1:8089/flowgate/api/v1/document/flowgate.default.0492.0010-L/reviews","rejection_history":[{"rejection_id":"rej_01M1CB4A4MXMPZSE","reason":"## Automated review rejection\n\nR4/R5/R6의 축 정의, 서버 조립 순서, 진단 로그 정책 등 큰 골격은 D0005/NR0003과 정합합니다. 다만 이 문서가 다루는 register_document 실패 표면이 이미 승인된 P0007(HTTP 403은 항상 code=forbidden, 원인은 details로만 구분)과 L0009(완료 도구 실패는 원인 불문 diagnostics 기록 후 다음 turn으로 계속, run을 종료하지 않음)의 확정 계약과 맞닿아 있는데, 이 문서는 그 두 계약과 반대되거나 반영하지 않은 새 규칙(top-level context_binding_mismatch/permission_denied 코드, register 실패 시 즉시 run 종료)을 도입하면서 P0007/L0009 쪽 개정 필요성을 언급하지 않습니다. 또한 review의 legacy 완화 예외가 서술에만 있고 validate_inbox_binding 의사코드에는 반영되지 않았습니다. 세 지점을 P0007/L0009와 정합하도록 명시하거나, 두 상위 문서의 해당 부분이 함께 바뀌어야 함을 이 문서에서 못박아야 합니다.\n\n- 2.4 403별 식별 가능한 오류 표면 (forbidden_binding_response) 및 2.5 register 실패 처리의 permission_denied: 이미 승인된 P0007의 [공통 오류 코드] 표는 register_document(모든 action_scope)를 포함한 모든 tool_result 실패를 HTTP status 하나만 보고 고정된 enum(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)으로 정규화하며, 403은 항상 code=\"forbidden\"이고 세부 원인은 details로만 구분한다고 명시한다('code는 내부 응답의 HTTP status만 보고 아래 표로 고른다 — 어느 계층·어느 서비스가 잡았는지는 코드에 반영하지 않는다'). 그런데 이 문서(L0010)는 register 경로의 403 top-level error.code를 context_binding_mismatch로, 권한 부족 403은 별도로 permission_denied로 정의한다(2.4 forbidden_binding_response, 2.5). 이는 P0007이 고정한 7개 code 열거값 밖의 새 top-level code를 register_document 실패 표면에 도입하는 것으로, L0009가 정확히 같은 이유(group_worktree_unavailable/invalid_tool_call/toolset_unavailable을 top-level code처럼 쓰다가 반려되어 conflict/invalid_request/unavailable + details.reason으로 고친 전례)로 반려·수정된 패턴을 반복한다. context_binding_mismatch/permission_denied가 실제로 모델에게 가는 register_document tool_result의 top-level code인지, 아니면 P0007처럼 code=\"forbidden\" 아래 details.reason(또는 details.axis/axes와 별개의 필드)으로 들어가는 내부 원인 라벨인지 이 문서가 명시하지 않았다. 명시하지 않으면 L0009/P0007의 tool_result 정규화 계약과 이 문서의 raw 403 계약 중 무엇을 구현해야 하는지 알 수 없다.\n- 2.5 완료 처리와 불변식 — register 실패 시 run 종료: 이 문서는 \"register 실패 시 모델이 바인딩 필드를 고쳐 재시도하도록 유도하지 않고, register_errors에 status=403, reason=context_binding_mismatch, axis, axes, turn, correlation_id를 기록한 뒤 run을 fail-closed 종료한다\"고 규정한다. 그러나 이미 승인된 L0009 §2.7의 turn 루프 의사코드는 완료 도구(register_document) 호출이 실패하면 실패 원인과 무관하게 \"record diagnostics when completion call\" 후 \"break to next provider turn\"으로 처리하여 다음 provider turn으로 루프를 계속하고 run을 종료하지 않는다(run 종료는 오직 고정 컨텍스트 실패/취소/기한 같은 별도 failed 경로에서만 일어난다). context_binding_mismatch만 예외적으로 run을 즉시 종료하려면 L0009가 정의한 turn 루프에 이 분기(어떤 조건에서 diagnostics 기록 후 계속 진행하지 않고 곧바로 run을 종료로 전이시키는지)를 추가해야 하는데, 이 문서는 L0009의 루프를 전혀 참조하거나 개정 필요성을 언급하지 않는다. 두 문서를 그대로 구현하면 어느 쪽 동작이 맞는지 충돌한다.\n- 2.3 R6 — validate_inbox_binding 의사코드 대 review의 legacy 완화 서술: 본문은 \"review의 현행 doc_ref in (None, \\\"\\\", doc_id) 완화는 server-mediated register에 적용하지 않는다 ... 사람용 legacy token 호환이 필요하면 별도 token kind에서만 허용하고 binding_relaxed=true 감사 로그를 남기며 API provider run에는 진입시키지 않는다\"고 하여, review의 doc 축 완화 여부가 token kind에 따라 갈린다고 서술한다. 그러나 바로 위 validate_inbox_binding 의사코드는 token kind를 입력 파라미터로도, 분기 조건으로도 전혀 다루지 않고 canonicalize(action/project/group/doc) 후 compare_binding_axes만 수행한다. legacy token의 완화 예외를 이 의사코드의 어느 지점(actual 계산 전에 완화된 doc 값을 대입하는지, 아니면 compare_binding_axes 이후 doc 축 mismatch를 무시하는 별도 분기를 두는지)에 넣을지가 알고리즘에 없어, 서술한 예외 규칙과 확정한 알고리즘이 서로 어긋난다.\n\nGET http://127.0.0.1:8089/flowgate/api/v1/document/flowgate.default.0492.0010-L/reviews","rejected_at":"2026-09-01T01:41:05+09:00","rejected_by":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","ai_response":"1) P0007 공통 오류 계약에 맞춰 모든 HTTP 403의 top-level error.code를 forbidden으로 고정하고 context_binding_mismatch 및 permission_denied를 details.reason으로 이동했습니다. 2) L0009 §2.7에 맞춰 register_document 실패 시 diagnostics 기록 후 현재 turn을 끝내고 다음 provider turn으로 계속하며, 실패 자체로 run을 종료하지 않도록 상태·결정 트리·예외 처리를 수정했습니다. 3) validate_inbox_binding에 token_kind 입력과 human_legacy 전용 분기를 추가해 API provider run 제외 조건, 비교 후 doc mismatch 제거 지점, binding_relaxed 감사 로그를 알고리즘에 명시했습니다.","responded_at":"2026-09-01T01:44:52+09:00","response_recorded_by":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","response_revision_no":1,"review_id":504}],"created_at":"2026-09-01T01:34:15+09:00","updated_at":"2026-09-01T01:45:21+09:00","ai_review":{"id":504,"revision_no":0,"reviewer_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","verdict":"issues","finding_count":3,"findings":[{"locus":"2.4 403별 식별 가능한 오류 표면 (forbidden_binding_response) 및 2.5 register 실패 처리의 permission_denied","note":"이미 승인된 P0007의 [공통 오류 코드] 표는 register_document(모든 action_scope)를 포함한 모든 tool_result 실패를 HTTP status 하나만 보고 고정된 enum(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)으로 정규화하며, 403은 항상 code=\"forbidden\"이고 세부 원인은 details로만 구분한다고 명시한다('code는 내부 응답의 HTTP status만 보고 아래 표로 고른다 — 어느 계층·어느 서비스가 잡았는지는 코드에 반영하지 않는다'). 그런데 이 문서(L0010)는 register 경로의 403 top-level error.code를 context_binding_mismatch로, 권한 부족 403은 별도로 permission_denied로 정의한다(2.4 forbidden_binding_response, 2.5). 이는 P0007이 고정한 7개 code 열거값 밖의 새 top-level code를 register_document 실패 표면에 도입하는 것으로, L0009가 정확히 같은 이유(group_worktree_unavailable/invalid_tool_call/toolset_unavailable을 top-level code처럼 쓰다가 반려되어 conflict/invalid_request/unavailable + details.reason으로 고친 전례)로 반려·수정된 패턴을 반복한다. context_binding_mismatch/permission_denied가 실제로 모델에게 가는 register_document tool_result의 top-level code인지, 아니면 P0007처럼 code=\"forbidden\" 아래 details.reason(또는 details.axis/axes와 별개의 필드)으로 들어가는 내부 원인 라벨인지 이 문서가 명시하지 않았다. 명시하지 않으면 L0009/P0007의 tool_result 정규화 계약과 이 문서의 raw 403 계약 중 무엇을 구현해야 하는지 알 수 없다."},{"locus":"2.5 완료 처리와 불변식 — register 실패 시 run 종료","note":"이 문서는 \"register 실패 시 모델이 바인딩 필드를 고쳐 재시도하도록 유도하지 않고, register_errors에 status=403, reason=context_binding_mismatch, axis, axes, turn, correlation_id를 기록한 뒤 run을 fail-closed 종료한다\"고 규정한다. 그러나 이미 승인된 L0009 §2.7의 turn 루프 의사코드는 완료 도구(register_document) 호출이 실패하면 실패 원인과 무관하게 \"record diagnostics when completion call\" 후 \"break to next provider turn\"으로 처리하여 다음 provider turn으로 루프를 계속하고 run을 종료하지 않는다(run 종료는 오직 고정 컨텍스트 실패/취소/기한 같은 별도 failed 경로에서만 일어난다). context_binding_mismatch만 예외적으로 run을 즉시 종료하려면 L0009가 정의한 turn 루프에 이 분기(어떤 조건에서 diagnostics 기록 후 계속 진행하지 않고 곧바로 run을 종료로 전이시키는지)를 추가해야 하는데, 이 문서는 L0009의 루프를 전혀 참조하거나 개정 필요성을 언급하지 않는다. 두 문서를 그대로 구현하면 어느 쪽 동작이 맞는지 충돌한다."},{"locus":"2.3 R6 — validate_inbox_binding 의사코드 대 review의 legacy 완화 서술","note":"본문은 \"review의 현행 doc_ref in (None, \\\"\\\", doc_id) 완화는 server-mediated register에 적용하지 않는다 ... 사람용 legacy token 호환이 필요하면 별도 token kind에서만 허용하고 binding_relaxed=true 감사 로그를 남기며 API provider run에는 진입시키지 않는다\"고 하여, review의 doc 축 완화 여부가 token kind에 따라 갈린다고 서술한다. 그러나 바로 위 validate_inbox_binding 의사코드는 token kind를 입력 파라미터로도, 분기 조건으로도 전혀 다루지 않고 canonicalize(action/project/group/doc) 후 compare_binding_axes만 수행한다. legacy token의 완화 예외를 이 의사코드의 어느 지점(actual 계산 전에 완화된 doc 값을 대입하는지, 아니면 compare_binding_axes 이후 doc 축 mismatch를 무시하는 별도 분기를 두는지)에 넣을지가 알고리즘에 없어, 서술한 예외 규칙과 확정한 알고리즘이 서로 어긋난다."}],"comment":"R4/R5/R6의 축 정의, 서버 조립 순서, 진단 로그 정책 등 큰 골격은 D0005/NR0003과 정합합니다. 다만 이 문서가 다루는 register_document 실패 표면이 이미 승인된 P0007(HTTP 403은 항상 code=forbidden, 원인은 details로만 구분)과 L0009(완료 도구 실패는 원인 불문 diagnostics 기록 후 다음 turn으로 계속, run을 종료하지 않음)의 확정 계약과 맞닿아 있는데, 이 문서는 그 두 계약과 반대되거나 반영하지 않은 새 규칙(top-level context_binding_mismatch/permission_denied 코드, register 실패 시 즉시 run 종료)을 도입하면서 P0007/L0009 쪽 개정 필요성을 언급하지 않습니다. 또한 review의 legacy 완화 예외가 서술에만 있고 validate_inbox_binding 의사코드에는 반영되지 않았습니다. 세 지점을 P0007/L0009와 정합하도록 명시하거나, 두 상위 문서의 해당 부분이 함께 바뀌어야 함을 이 문서에서 못박아야 합니다.","reviewed_at":"2026-09-01T01:40:55+09:00","created_at":"2026-09-01T01:40:55+09:00"},"ai_review_history":[{"id":504,"revision_no":0,"reviewer_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","verdict":"issues","finding_count":3,"findings":[{"locus":"2.4 403별 식별 가능한 오류 표면 (forbidden_binding_response) 및 2.5 register 실패 처리의 permission_denied","note":"이미 승인된 P0007의 [공통 오류 코드] 표는 register_document(모든 action_scope)를 포함한 모든 tool_result 실패를 HTTP status 하나만 보고 고정된 enum(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)으로 정규화하며, 403은 항상 code=\"forbidden\"이고 세부 원인은 details로만 구분한다고 명시한다('code는 내부 응답의 HTTP status만 보고 아래 표로 고른다 — 어느 계층·어느 서비스가 잡았는지는 코드에 반영하지 않는다'). 그런데 이 문서(L0010)는 register 경로의 403 top-level error.code를 context_binding_mismatch로, 권한 부족 403은 별도로 permission_denied로 정의한다(2.4 forbidden_binding_response, 2.5). 이는 P0007이 고정한 7개 code 열거값 밖의 새 top-level code를 register_document 실패 표면에 도입하는 것으로, L0009가 정확히 같은 이유(group_worktree_unavailable/invalid_tool_call/toolset_unavailable을 top-level code처럼 쓰다가 반려되어 conflict/invalid_request/unavailable + details.reason으로 고친 전례)로 반려·수정된 패턴을 반복한다. context_binding_mismatch/permission_denied가 실제로 모델에게 가는 register_document tool_result의 top-level code인지, 아니면 P0007처럼 code=\"forbidden\" 아래 details.reason(또는 details.axis/axes와 별개의 필드)으로 들어가는 내부 원인 라벨인지 이 문서가 명시하지 않았다. 명시하지 않으면 L0009/P0007의 tool_result 정규화 계약과 이 문서의 raw 403 계약 중 무엇을 구현해야 하는지 알 수 없다."},{"locus":"2.5 완료 처리와 불변식 — register 실패 시 run 종료","note":"이 문서는 \"register 실패 시 모델이 바인딩 필드를 고쳐 재시도하도록 유도하지 않고, register_errors에 status=403, reason=context_binding_mismatch, axis, axes, turn, correlation_id를 기록한 뒤 run을 fail-closed 종료한다\"고 규정한다. 그러나 이미 승인된 L0009 §2.7의 turn 루프 의사코드는 완료 도구(register_document) 호출이 실패하면 실패 원인과 무관하게 \"record diagnostics when completion call\" 후 \"break to next provider turn\"으로 처리하여 다음 provider turn으로 루프를 계속하고 run을 종료하지 않는다(run 종료는 오직 고정 컨텍스트 실패/취소/기한 같은 별도 failed 경로에서만 일어난다). context_binding_mismatch만 예외적으로 run을 즉시 종료하려면 L0009가 정의한 turn 루프에 이 분기(어떤 조건에서 diagnostics 기록 후 계속 진행하지 않고 곧바로 run을 종료로 전이시키는지)를 추가해야 하는데, 이 문서는 L0009의 루프를 전혀 참조하거나 개정 필요성을 언급하지 않는다. 두 문서를 그대로 구현하면 어느 쪽 동작이 맞는지 충돌한다."},{"locus":"2.3 R6 — validate_inbox_binding 의사코드 대 review의 legacy 완화 서술","note":"본문은 \"review의 현행 doc_ref in (None, \\\"\\\", doc_id) 완화는 server-mediated register에 적용하지 않는다 ... 사람용 legacy token 호환이 필요하면 별도 token kind에서만 허용하고 binding_relaxed=true 감사 로그를 남기며 API provider run에는 진입시키지 않는다\"고 하여, review의 doc 축 완화 여부가 token kind에 따라 갈린다고 서술한다. 그러나 바로 위 validate_inbox_binding 의사코드는 token kind를 입력 파라미터로도, 분기 조건으로도 전혀 다루지 않고 canonicalize(action/project/group/doc) 후 compare_binding_axes만 수행한다. legacy token의 완화 예외를 이 의사코드의 어느 지점(actual 계산 전에 완화된 doc 값을 대입하는지, 아니면 compare_binding_axes 이후 doc 축 mismatch를 무시하는 별도 분기를 두는지)에 넣을지가 알고리즘에 없어, 서술한 예외 규칙과 확정한 알고리즘이 서로 어긋난다."}],"comment":"R4/R5/R6의 축 정의, 서버 조립 순서, 진단 로그 정책 등 큰 골격은 D0005/NR0003과 정합합니다. 다만 이 문서가 다루는 register_document 실패 표면이 이미 승인된 P0007(HTTP 403은 항상 code=forbidden, 원인은 details로만 구분)과 L0009(완료 도구 실패는 원인 불문 diagnostics 기록 후 다음 turn으로 계속, run을 종료하지 않음)의 확정 계약과 맞닿아 있는데, 이 문서는 그 두 계약과 반대되거나 반영하지 않은 새 규칙(top-level context_binding_mismatch/permission_denied 코드, register 실패 시 즉시 run 종료)을 도입하면서 P0007/L0009 쪽 개정 필요성을 언급하지 않습니다. 또한 review의 legacy 완화 예외가 서술에만 있고 validate_inbox_binding 의사코드에는 반영되지 않았습니다. 세 지점을 P0007/L0009와 정합하도록 명시하거나, 두 상위 문서의 해당 부분이 함께 바뀌어야 함을 이 문서에서 못박아야 합니다.","reviewed_at":"2026-09-01T01:40:55+09:00","created_at":"2026-09-01T01:40:55+09:00"}],"test_run":null,"test_run_history":[]}