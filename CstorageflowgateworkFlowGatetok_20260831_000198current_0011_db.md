{"ok":true,"doc_id":"flowgate.default.0492.0011-DB","type":"DB","title":"API 프로바이더 register 실패 텔레메트리·capability 메타데이터 DB설계","status":"open","revision_no":1,"owner_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","triggered_by":"flowgate.default.0492.0001-B","group_id":"flowgate.default.0492","project":"flowgate","module":"default","branch":"main","stored_path":"documents/flowgate/main/default/0492/0011-DB_document.md","content":"# API 프로바이더 register 실패 텔레메트리·capability 메타데이터 DB설계 (DB)\n\n> 단계: DB설계(DB) — WP0004 L#4\n> 참조: L0010, L0009, P0007, D0005, NR0003 R4·R5·R6\n\n---\n\n## 목적\n\nL0010의 server-mediated `register_document` 컨텍스트 바인딩 실패와 L0009의 API 프로바이더 도구 디스패치 오류를 기계적으로 식별·추적할 수 있도록, 바인딩 실패 텔레메트리(run/token action_scope, 요청 action, run doc_ref, 대상 doc, group, ai_run_id 일치, mismatch 축)와 프로바이더 capability 메타데이터를 저장할 테이블과 컬럼을 설계한다. 또한 기존 ai_invoke_runs 행의 register_errors 배열에서 구조화된 개별 레코드로 마이그레이션하는 절차를 확정한다.\n\n---\n\n## 1. 대상 테이블\n\n| 테이블 | 신설/변경 | 목적 |\n|---|---|---|\n| `register_context_failures` | 신설 | L0010 §2.4의 context-binding 403 실패 로그. dispatcher/inbox 경계에서 실패한 register 요청의 네 축(action/project/group/doc) 불일치를 기록. |\n| `provider_capabilities` | 신설 | L0009 resolve_turn_tools에서 참조할 프로바이더별 도구셋 설정. provider id, step type (T/TR/NR), mutation tier 지원 여부, 노출 도구 목록을 저장. |\n| `ai_invoke_runs` | 변경 | 기존 register_errors JSONB를 제거하고 register_context_failures FK를 추가. run id별로 최대 M개 텔레메트리 행을 갖는 관계 테이블로 전환. |\n\n---\n\n## 2. 컬럼·키·제약\n\n### 2.1 register_context_failures 테이블\n\n| 컬럼 | 타입 | NULL | 기본값 | 제약(PK/FK/UQ/CHECK) | 설명 |\n|---|---|---|---|---|---|\n| `id` | BIGINT | N | generated | PK, auto-increment | 텔레메트리 행 고유 id |\n| `recorded_at` | TIMESTAMP WITH TIME ZONE | N | CURRENT_TIMESTAMP | INDEX(recorded_at DESC) | 실패 발생 시각. 로그 추적 및 분석 범위 쿼리용. |\n| `run_id` | UUID | N | | FK(ai_invoke_runs.id) ON DELETE CASCADE | 실패한 provider turn의 owner run. |\n| `correlation_id` | UUID | N | | INDEX(correlation_id) | HTTP 요청 추적용. dispatcher와 inbox 경계를 거쳐 동일 id 전파. |\n| `boundary` | VARCHAR(32) | N | | CHECK (boundary IN ('dispatcher', 'inbox')) | 실패 지점: dispatcher (R5 run/token 비교) 또는 inbox (R6 재검증). |\n| `action_scope_run` | VARCHAR(32) | N | | | run.action_scope의 정규화 값. new/edit/review/test_run/workflow_decide/resolve_conflict/chat. |\n| `action_scope_token` | VARCHAR(32) | N | | | token.action_scope의 정규화 값. 스코프 불일치 진단용. |\n| `action_scope_request` | VARCHAR(32) | Y | NULL | | register 요청의 action 필드. (모델 입력 사용 금지이므로 항상 NULL. dispatcher에서만 값 채우는 unused 컬럼이지만 구조화 로깅 호환성 위해 보유.) |\n| `project_run` | VARCHAR(128) | N | | | run.project_id의 정규화 값. |\n| `project_token` | VARCHAR(128) | N | | | token.project의 정규화 값. |\n| `group_run` | VARCHAR(256) | N | | | run.group_id의 정규화 값. (완전한 group id.) |\n| `group_token_db` | VARCHAR(256) | Y | NULL | | token.group 값. 레거시 토큰의 경우 NULL이고 token.doc_ref 소속 group으로 보완됨. |\n| `group_token_resolved` | VARCHAR(256) | N | | | token.group 또는 doc_ref 소속 group의 정규화 값. |\n| `doc_ref_run` | VARCHAR(512) | N | | | run.doc_ref의 정규화 값. (완전한 document id.) |\n| `doc_ref_token` | VARCHAR(512) | N | | | token.doc_ref의 정규화 값. new에서는 prev_doc_id, 그 외는 target doc_id와 비교 대상. |\n| `prev_doc_id_request` | VARCHAR(512) | Y | NULL | | action=new인 경우 request.prev_doc_id. 그 외 NULL. |\n| `target_doc_id_request` | VARCHAR(512) | Y | NULL | | action=edit/review/test_run인 경우 request.doc_id. 그 외 NULL. |\n| `ai_run_id` | VARCHAR(128) | Y | NULL | | 이 run의 ai_run_id (서버 발급 run id). run_id와의 일대일 매핑. |\n| `axis_first_mismatch` | VARCHAR(32) | N | | CHECK (axis_first_mismatch IN ('action', 'project', 'group', 'doc')) | 고정 순서의 첫 불일치 축 (L0010 binding_axis_order). |\n| `axes_all_mismatches` | VARCHAR(256) | N | | | JSON 배열 문자열 [\\\"action\\\", \\\"group\\\"] 형태. 전체 불일치 축 목록. |\n| `token_id_hash` | VARCHAR(128) | Y | NULL | | token.token_id의 비가역 해시값. 원문 토큰 ID 비노출. |\n| `expected_fingerprint` | VARCHAR(128) | Y | NULL | | run 컨텍스트 네 축의 HMAC(server_secret). 재현성 확인용. |\n| `actual_fingerprint` | VARCHAR(128) | Y | NULL | | token 컨텍스트 네 축의 HMAC(server_secret). |\n| `binding_relaxed` | BOOLEAN | N | false | CHECK (NOT binding_relaxed OR relaxed_axis = 'doc') | review + human_legacy token에서만 doc 축을 제거한 경우 true. relaxed_axis와의 양방향 조건. |\n| `relaxed_axis` | VARCHAR(32) | Y | NULL | CHECK (relaxed_axis IS NULL OR (relaxed_axis = 'doc' AND binding_relaxed)) | binding_relaxed=true인 경우 'doc', 그 외 NULL. |\n| `notes` | TEXT | Y | NULL | | 추가 진단 노트. (내부용.) |\n\n**PK:** `(id)`\n**FK:** `(run_id)` → `ai_invoke_runs(id)`\n**인덱스:**\n- `UNIQUE(correlation_id)` — 요청 추적 id는 중복 없어야 함.\n- `(run_id, recorded_at DESC)` — run별 타임라인 쿼리.\n- `(boundary, axis_first_mismatch)` — 실패 지점·원인별 분류.\n- `(recorded_at DESC)` — 전체 로그 범위 쿼리.\n\n---\n\n### 2.2 provider_capabilities 테이블\n\n| 컬럼 | 타입 | NULL | 기본값 | 제약(PK/FK/UQ/CHECK) | 설명 |\n|---|---|---|---|---|---|\n| `id` | BIGINT | N | generated | PK, auto-increment | 설정 행 고유 id. |\n| `provider_id` | VARCHAR(64) | N | | UQ(provider_id, step_type, disabled_at IS NULL) | L0009에 명시된 프로바이더 식별자. 예: 'anthropic', 'openai'. |\n| `step_type` | VARCHAR(32) | N | | CHECK (step_type IN ('NR', 'T', 'TR', 'other')) | 워크플로우 문서 타입. 프로바이더별로 허용 step을 제한. |\n| `mutation_tier_enabled` | BOOLEAN | N | false | CHECK (NOT mutation_tier_enabled OR mutation_tool_names IS NOT NULL) | T/TR의 mutation_tool_names (read_source_file, write_source_file, patch_source_file, run_test) 노출 여부. mutation_tool_names가 있으면 true여야 함. |\n| `base_tool_names` | JSONB | N | | | JSON 배열. 항상 노출되는 기본 도구명: [\\\"read_document\\\", \\\"create_question\\\", \\\"register_document\\\"]. L0009 base_tool_names 상수. |\n| `mutation_tool_names` | JSONB | Y | NULL | CHECK (mutation_tool_names IS NULL OR mutation_tier_enabled) | JSON 배열. mutation_tier_enabled=true일 때만 있음. [\\\"read_source_file\\\", \\\"search_source\\\", \\\"patch_source_file\\\", \\\"write_source_file\\\", \\\"run_test\\\"]. |\n| `test_timeout_default_sec` | INTEGER | N | 300 | CHECK (test_timeout_default_sec >= 1 AND test_timeout_default_sec <= 600) | 이 프로바이더의 run_test 기본 타임아웃. 범위: [1, 600]. L0009 test_timeout_default. |\n| `api_call_timeout_max_sec` | INTEGER | N | 600 | CHECK (api_call_timeout_max_sec >= 30 AND api_call_timeout_max_sec <= 3600) | provider API 호출 상한. 범위: [30, 3600]. |\n| `enabled_at` | TIMESTAMP WITH TIME ZONE | N | CURRENT_TIMESTAMP | | 이 설정이 활성화된 시각. |\n| `disabled_at` | TIMESTAMP WITH TIME ZONE | Y | NULL | | 비활성화 시각. NULL이면 현재 활성. |\n| `notes` | TEXT | Y | NULL | | 설정 근거/변경 사항 기록용. |\n\n**PK:** `(id)`\n**UQ:** `(provider_id, step_type) WHERE disabled_at IS NULL` — 활성 설정만 provider/step 쌍당 1개. 비활성 historical 행은 중복 허용.\n**인덱스:**\n- `(provider_id, disabled_at) WHERE disabled_at IS NULL` — provider별 활성 설정 조회.\n- `(step_type, enabled_at DESC)` — step별 설정 타임라인.\n\n---\n\n### 2.3 ai_invoke_runs 테이블 변경\n\n| 컬럼 | 변경 | 설명 |\n|---|---|---|\n| `id` | 유지 | UUID PK. |\n| `ai_run_id` | 유지 | 서버 발급 run id. |\n| `project_id` | 유지 | run의 프로젝트. |\n| `group_id` | 유지 | run의 그룹. |\n| `doc_ref` | 유지 | run의 현재 문서 id. |\n| `action_scope` | 유지 | run의 action scope. |\n| `register_errors` | 제거 | 기존 JSONB 배열 필드를 삭제. register_context_failures FK로 전환. |\n| `register_failure_ids` | 추가 | BIGINT 배열 (또는 관계 테이블로 대체). register_context_failures.id 목록. 읽기 최적화용 denormalize. |\n\n**마이그레이션 전략:**\n- 기존 `register_errors[*]` 배열의 각 원소를 register_context_failures 행으로 변환.\n- `recorded_at` ← `COALESCE((error_obj->>'recorded_at')::timestamp with time zone, air.updated_at AT TIME ZONE 'UTC')` 사용.\n- `correlation_id` ← 새로 UUID 생성 (기존 데이터는 NULL).\n- `original_payload` ← 기존 JSON 전체 보존 (역변환 및 향후 분석용).\n- `status`, `reason`, `turn` 등 L0009 정의 필드는 notes 또는 별도 컬럼에 저장.\n- register_failure_ids 배열은 생성된 register_context_failures.id들로 채움.\n\n---\n\n## 3. 마이그레이션\n\n### 마이그레이션 3-1: 테이블 신설 (0001-register-telemetry-tables.sql)\n\n```sql\nCREATE TABLE register_context_failures (\n    id BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,\n    recorded_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    run_id UUID NOT NULL,\n    correlation_id UUID NOT NULL UNIQUE,\n    boundary VARCHAR(32) NOT NULL CHECK (boundary IN ('dispatcher', 'inbox')),\n    \n    action_scope_run VARCHAR(32) NOT NULL,\n    action_scope_token VARCHAR(32) NOT NULL,\n    action_scope_request VARCHAR(32),\n    \n    project_run VARCHAR(128) NOT NULL,\n    project_token VARCHAR(128) NOT NULL,\n    \n    group_run VARCHAR(256) NOT NULL,\n    group_token_db VARCHAR(256),\n    group_token_resolved VARCHAR(256) NOT NULL,\n    \n    doc_ref_run VARCHAR(512) NOT NULL,\n    doc_ref_token VARCHAR(512) NOT NULL,\n    prev_doc_id_request VARCHAR(512),\n    target_doc_id_request VARCHAR(512),\n    \n    ai_run_id VARCHAR(128),\n    axis_first_mismatch VARCHAR(32) NOT NULL CHECK (axis_first_mismatch IN ('action', 'project', 'group', 'doc')),\n    axes_all_mismatches VARCHAR(256) NOT NULL, -- JSON array string\n    token_id_hash VARCHAR(128),\n    expected_fingerprint VARCHAR(128),\n    actual_fingerprint VARCHAR(128),\n    binding_relaxed BOOLEAN NOT NULL DEFAULT false,\n    relaxed_axis VARCHAR(32) CHECK (relaxed_axis IS NULL OR (relaxed_axis = 'doc' AND binding_relaxed)),\n    notes TEXT,\n    \n    FOREIGN KEY (run_id) REFERENCES ai_invoke_runs(id) ON DELETE CASCADE,\n    CONSTRAINT check_binding_relaxed_requires_axis CHECK (NOT binding_relaxed OR relaxed_axis = 'doc')\n);\n\nCREATE INDEX idx_register_failures_run_time ON register_context_failures(run_id, recorded_at DESC);\nCREATE INDEX idx_register_failures_boundary_axis ON register_context_failures(boundary, axis_first_mismatch);\nCREATE INDEX idx_register_failures_recorded ON register_context_failures(recorded_at DESC);\nCREATE INDEX idx_register_failures_correlation ON register_context_failures(correlation_id);\n\nCREATE TABLE provider_capabilities (\n    id BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,\n    provider_id VARCHAR(64) NOT NULL,\n    step_type VARCHAR(32) NOT NULL CHECK (step_type IN ('NR', 'T', 'TR', 'other')),\n    mutation_tier_enabled BOOLEAN NOT NULL DEFAULT false,\n    base_tool_names JSONB NOT NULL,\n    mutation_tool_names JSONB,\n    test_timeout_default_sec INTEGER NOT NULL CHECK (test_timeout_default_sec >= 1 AND test_timeout_default_sec <= 600) DEFAULT 300,\n    api_call_timeout_max_sec INTEGER NOT NULL CHECK (api_call_timeout_max_sec >= 30 AND api_call_timeout_max_sec <= 3600) DEFAULT 600,\n    enabled_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    disabled_at TIMESTAMP WITH TIME ZONE,\n    notes TEXT,\n    \n    CONSTRAINT check_mutation_requires_tools CHECK (NOT mutation_tier_enabled OR mutation_tool_names IS NOT NULL),\n    CONSTRAINT check_mutation_tools_requires_flag CHECK (mutation_tool_names IS NULL OR mutation_tier_enabled),\n    UNIQUE (provider_id, step_type) WHERE disabled_at IS NULL\n);\n\nCREATE UNIQUE INDEX idx_provider_caps_active ON provider_capabilities(provider_id, step_type) WHERE disabled_at IS NULL;\nCREATE INDEX idx_provider_caps_step ON provider_capabilities(step_type, enabled_at DESC);\n```\n\n**가역성:** 테이블 삭제로 롤백 가능. 신설 테이블이고 기존 데이터와 무관.\n\n### 마이그레이션 3-2: ai_invoke_runs 칼럼 추가 및 데이터 마이그레이션 (0002-ai-invoke-runs-register-telemetry.sql)\n\n```sql\n-- 1단계: register_failure_ids와 original_register_errors 컬럼 추가\nALTER TABLE ai_invoke_runs\n    ADD COLUMN register_failure_ids BIGINT[] DEFAULT '{}',\n    ADD COLUMN original_register_errors JSONB;\n\n-- 2단계: 기존 register_errors 배열에서 register_context_failures로 데이터 이전\nINSERT INTO register_context_failures (\n    run_id, correlation_id, boundary, action_scope_run, action_scope_token,\n    action_scope_request, project_run, project_token, group_run, group_token_db,\n    group_token_resolved, doc_ref_run, doc_ref_token, prev_doc_id_request,\n    target_doc_id_request, ai_run_id, axis_first_mismatch, axes_all_mismatches,\n    token_id_hash, expected_fingerprint, actual_fingerprint, binding_relaxed,\n    relaxed_axis, notes, recorded_at\n)\nSELECT\n    air.id,\n    gen_random_uuid(),\n    COALESCE(error_obj->>'boundary', 'inbox')::VARCHAR(32),\n    air.action_scope,\n    COALESCE(error_obj->>'action_scope_token', air.action_scope)::VARCHAR(32),\n    NULL,\n    air.project_id,\n    COALESCE(error_obj->>'project_token', air.project_id)::VARCHAR(128),\n    air.group_id,\n    NULLIF(error_obj->>'group_token_db', '')::VARCHAR(256),\n    COALESCE(error_obj->>'group_token_resolved', air.group_id)::VARCHAR(256),\n    air.doc_ref,\n    COALESCE(error_obj->>'doc_ref_token', air.doc_ref)::VARCHAR(512),\n    NULLIF(error_obj->>'prev_doc_id_request', '')::VARCHAR(512),\n    NULLIF(error_obj->>'target_doc_id_request', '')::VARCHAR(512),\n    air.ai_run_id,\n    COALESCE(error_obj->>'axis_first_mismatch', 'action')::VARCHAR(32),\n    COALESCE(error_obj->>'axes_all_mismatches', '[\\\"action\\\"]')::VARCHAR(256),\n    NULLIF(error_obj->>'token_id_hash', '')::VARCHAR(128),\n    NULLIF(error_obj->>'expected_fingerprint', '')::VARCHAR(128),\n    NULLIF(error_obj->>'actual_fingerprint', '')::VARCHAR(128),\n    COALESCE((error_obj->>'binding_relaxed')::boolean, false),\n    CASE WHEN COALESCE((error_obj->>'binding_relaxed')::boolean, false) THEN 'doc'::VARCHAR(32) ELSE NULL END,\n    error_obj->>'notes',\n    COALESCE(\n        (error_obj->>'recorded_at')::timestamp with time zone,\n        air.updated_at AT TIME ZONE 'UTC'\n    )\nFROM\n    ai_invoke_runs air,\n    LATERAL jsonb_array_elements(COALESCE(air.register_errors, '[]'::jsonb)) AS error_obj\nWHERE\n    air.register_errors IS NOT NULL AND jsonb_array_length(air.register_errors) > 0;\n\n-- 3단계: 기존 register_errors를 original_register_errors로 백업\nUPDATE ai_invoke_runs\nSET original_register_errors = register_errors\nWHERE register_errors IS NOT NULL;\n\n-- 4단계: ai_invoke_runs.register_failure_ids 업데이트\nUPDATE ai_invoke_runs air\nSET register_failure_ids = (\n    SELECT ARRAY_AGG(rcf.id ORDER BY rcf.recorded_at ASC)\n    FROM register_context_failures rcf\n    WHERE rcf.run_id = air.id\n)\nWHERE EXISTS (\n    SELECT 1 FROM register_context_failures rcf WHERE rcf.run_id = air.id\n);\n\n-- 5단계: register_errors 컬럼 삭제\nALTER TABLE ai_invoke_runs\n    DROP COLUMN register_errors;\n```\n\n**가역성:** 마이그레이션 전 register_errors 배열이 original_register_errors로 백업됨. 롤백 스크립트:\n```sql\nALTER TABLE ai_invoke_runs ADD COLUMN register_errors JSONB;\nUPDATE ai_invoke_runs SET register_errors = original_register_errors WHERE original_register_errors IS NOT NULL;\nALTER TABLE ai_invoke_runs DROP COLUMN original_register_errors;\nALTER TABLE ai_invoke_runs DROP COLUMN register_failure_ids;\nDELETE FROM register_context_failures;\n```\n\n**데이터 보존:** 기존 register_errors JSONB의 모든 필드는 register_context_failures의 해당 컬럼 또는 notes로 변환. original_register_errors 백업으로 JSON 손실 없음. L0009 정의 `status`, `reason`, `turn` 필드도 notes 문자열에 포함.\n\n### 마이그레이션 3-3: provider_capabilities 초기 데이터 (0003-provider-capabilities-seed.sql)\n\n```sql\nINSERT INTO provider_capabilities (provider_id, step_type, mutation_tier_enabled, base_tool_names, mutation_tool_names, notes)\nVALUES\n    ('anthropic', 'NR', false, '[\"read_document\", \"create_question\", \"register_document\"]'::jsonb, NULL, 'NR 기본 tier만 노출'),\n    ('anthropic', 'T', true, '[\"read_document\", \"create_question\", \"register_document\"]'::jsonb, '[\"read_source_file\", \"search_source\", \"patch_source_file\", \"write_source_file\", \"run_test\"]'::jsonb, 'T: 전체 mutation tier'),\n    ('anthropic', 'TR', true, '[\"read_document\", \"create_question\", \"register_document\"]'::jsonb, '[\"read_source_file\", \"search_source\", \"patch_source_file\", \"write_source_file\", \"run_test\"]'::jsonb, 'TR: 전체 mutation tier'),\n    ('openai', 'NR', false, '[\"read_document\", \"create_question\", \"register_document\"]'::jsonb, NULL, 'NR 기본 tier만 노출'),\n    ('openai', 'T', true, '[\"read_document\", \"create_question\", \"register_document\"]'::jsonb, '[\"read_source_file\", \"search_source\", \"patch_source_file\", \"write_source_file\", \"run_test\"]'::jsonb, 'T: 전체 mutation tier'),\n    ('openai', 'TR', true, '[\"read_document\", \"create_question\", \"register_document\"]'::jsonb, '[\"read_source_file\", \"search_source\", \"patch_source_file\", \"write_source_file\", \"run_test\"]'::jsonb, 'TR: 전체 mutation tier');\n```\n\n**가역성:** DELETE로 롤백 가능. seed 데이터만 삭제됨.\n\n---\n\n## 4. 읽기/쓰기 쿼리\n\n### 4.1 register_context_failures 쓰기 경로\n\n**경로 1: dispatcher 실패 기록 (L0010 R5)**\n\n```sql\nINSERT INTO register_context_failures (\n    run_id, correlation_id, boundary, action_scope_run, action_scope_token,\n    project_run, project_token, group_run, group_token_db, group_token_resolved,\n    doc_ref_run, doc_ref_token, prev_doc_id_request, target_doc_id_request,\n    ai_run_id, axis_first_mismatch, axes_all_mismatches,\n    token_id_hash, expected_fingerprint, actual_fingerprint,\n    binding_relaxed, relaxed_axis, notes\n)\nVALUES (\n    $1::UUID, $2::UUID, 'dispatcher',\n    $3::VARCHAR, $4::VARCHAR, $5::VARCHAR, $6::VARCHAR,\n    $7::VARCHAR, $8::VARCHAR, $9::VARCHAR,\n    $10::VARCHAR, $11::VARCHAR, $12::VARCHAR, $13::VARCHAR,\n    $14::VARCHAR, $15::VARCHAR, $16::VARCHAR,\n    $17::VARCHAR, $18::VARCHAR, $19::VARCHAR,\n    $20::BOOLEAN, $21::VARCHAR, $22::TEXT\n);\n```\n\n**경로 2: inbox 실패 기록 (L0010 R6)**\n\n```sql\nINSERT INTO register_context_failures (\n    run_id, correlation_id, boundary, action_scope_run, action_scope_token,\n    project_run, project_token, group_run, group_token_db, group_token_resolved,\n    doc_ref_run, doc_ref_token, prev_doc_id_request, target_doc_id_request,\n    ai_run_id, axis_first_mismatch, axes_all_mismatches,\n    token_id_hash, expected_fingerprint, actual_fingerprint,\n    binding_relaxed, relaxed_axis, notes\n)\nVALUES (\n    $1::UUID, $2::UUID, 'inbox',\n    $3::VARCHAR, $4::VARCHAR, $5::VARCHAR, $6::VARCHAR,\n    $7::VARCHAR, $8::VARCHAR, $9::VARCHAR,\n    $10::VARCHAR, $11::VARCHAR, $12::VARCHAR, $13::VARCHAR,\n    $14::VARCHAR, $15::VARCHAR, $16::VARCHAR,\n    $17::VARCHAR, $18::VARCHAR, $19::VARCHAR,\n    $20::BOOLEAN, $21::VARCHAR, $22::TEXT\n);\n```\n\n**경로 3: ai_invoke_runs.register_failure_ids 업데이트**\n\n```sql\nUPDATE ai_invoke_runs\nSET register_failure_ids = register_failure_ids || ARRAY[$1::BIGINT]\nWHERE id = $2::UUID;\n```\n\n### 4.2 register_context_failures 읽기 경로\n\n**쿼리 1: 단일 run의 모든 실패 로그**\n\n```sql\nSELECT id, recorded_at, boundary, axis_first_mismatch, axes_all_mismatches,\n       action_scope_run, action_scope_token, project_run, project_token,\n       group_run, group_token_resolved, doc_ref_run, doc_ref_token,\n       binding_relaxed, relaxed_axis\nFROM register_context_failures\nWHERE run_id = $1::UUID\nORDER BY recorded_at DESC;\n```\n\n**쿼리 2: 실패 축별 분류**\n\n```sql\nSELECT axis_first_mismatch, COUNT(*) as failure_count\nFROM register_context_failures\nWHERE recorded_at > $1::TIMESTAMP WITH TIME ZONE\n  AND boundary = $2::VARCHAR\nGROUP BY axis_first_mismatch\nORDER BY failure_count DESC;\n```\n\n**쿼리 3: dispatcher vs inbox 경계별 실패율**\n\n```sql\nSELECT boundary, COUNT(*) as total_failures,\n       COUNT(*) FILTER (WHERE binding_relaxed) as relaxed_review_count,\n       COUNT(DISTINCT run_id) as unique_runs\nFROM register_context_failures\nWHERE recorded_at > $1::TIMESTAMP WITH TIME ZONE\nGROUP BY boundary;\n```\n\n### 4.3 provider_capabilities 읽기 경로\n\n**쿼리 1: 현재 활성 프로바이더 설정**\n\n```sql\nSELECT id, provider_id, step_type, mutation_tier_enabled,\n       base_tool_names, mutation_tool_names,\n       test_timeout_default_sec, api_call_timeout_max_sec\nFROM provider_capabilities\nWHERE disabled_at IS NULL\n  AND provider_id = $1::VARCHAR\n  AND step_type = $2::VARCHAR;\n```\n\n**쿼리 2: 모든 활성 설정 (L0009 resolve_turn_tools용)**\n\n```sql\nSELECT provider_id, step_type, mutation_tier_enabled,\n       base_tool_names, mutation_tool_names\nFROM provider_capabilities\nWHERE disabled_at IS NULL\nORDER BY provider_id, step_type;\n```\n\n**쿼리 3: 프로바이더 타임라인**\n\n```sql\nSELECT id, enabled_at, disabled_at, mutation_tier_enabled, notes\nFROM provider_capabilities\nWHERE provider_id = $1::VARCHAR\nORDER BY enabled_at DESC;\n```\n\n---\n\n## 5. 무결성·롤백\n\n### 5.1 무결성 불변식\n\n| 불변식 | 강제 수단 | 적용 범위 |\n|---|---|---|\n| `register_context_failures.run_id` 존재 | FK(ai_invoke_runs) + ON DELETE CASCADE | 모든 행 |\n| `register_context_failures.correlation_id` 고유 | UNIQUE(correlation_id) | 모든 행 |\n| `axis_first_mismatch` ∈ {action, project, group, doc} | CHECK 제약 | 모든 행 |\n| `axes_all_mismatches` JSON 배열 형식 | JSON 파싱 (애플리케이션) | 모든 행 |\n| `axes_all_mismatches[0]` = `axis_first_mismatch` | 트리거 함수 검증 | 쓰기 시점 |\n| `boundary` ∈ {dispatcher, inbox} | CHECK 제약 | 모든 행 |\n| `binding_relaxed` = true ⇒ `relaxed_axis` = 'doc' | CHECK 제약 (`check_binding_relaxed_requires_axis`) | 모든 행 |\n| `relaxed_axis` IS NULL ⇒ `binding_relaxed` = false | CHECK 제약 | 모든 행 |\n| `provider_capabilities` (provider_id, step_type, disabled_at) 유니크 | 부분 UNIQUE INDEX (disabled_at IS NULL인 활성 행만) | 활성 행 |\n| `test_timeout_default_sec` ∈ [1, 600] | CHECK 제약 | 모든 행 |\n| `api_call_timeout_max_sec` ∈ [30, 3600] | CHECK 제약 | 모든 행 |\n| `mutation_tier_enabled` = true ⇔ `mutation_tool_names` NOT NULL | CHECK 제약 (양방향) | 모든 행 |\n| `base_tool_names`, `mutation_tool_names` JSONB 타입 | 타입 강제 + 애플리케이션 JSON 검증 | 모든 행 |\n\n### 5.2 트리거 (필수 구현)\n\n**트리거 1: register_context_failures validation**\n\n```sql\nCREATE OR REPLACE FUNCTION validate_register_failure()\nRETURNS TRIGGER AS $$\nBEGIN\n    -- axes_all_mismatches JSON 배열 첫 요소가 axis_first_mismatch와 일치\n    IF NOT (NEW.axes_all_mismatches::jsonb->>0 = NEW.axis_first_mismatch) THEN\n        RAISE EXCEPTION 'axis_first_mismatch must match axes_all_mismatches[0]';\n    END IF;\n    \n    -- binding_relaxed = true ⇒ relaxed_axis = 'doc'\n    IF NEW.binding_relaxed AND (NEW.relaxed_axis IS NULL OR NEW.relaxed_axis != 'doc') THEN\n        RAISE EXCEPTION 'relaxed_axis must be doc when binding_relaxed is true';\n    END IF;\n    \n    -- relaxed_axis = 'doc' ⇒ binding_relaxed = true\n    IF NEW.relaxed_axis = 'doc' AND NOT NEW.binding_relaxed THEN\n        RAISE EXCEPTION 'binding_relaxed must be true when relaxed_axis is doc';\n    END IF;\n    \n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_validate_register_failure\n    BEFORE INSERT OR UPDATE ON register_context_failures\n    FOR EACH ROW\n    EXECUTE FUNCTION validate_register_failure();\n```\n\n**트리거 2: provider_capabilities validation**\n\n```sql\nCREATE OR REPLACE FUNCTION validate_provider_capability()\nRETURNS TRIGGER AS $$\nBEGIN\n    -- mutation_tool_names NOT NULL ⇒ mutation_tier_enabled = true\n    IF NEW.mutation_tool_names IS NOT NULL AND NOT NEW.mutation_tier_enabled THEN\n        RAISE EXCEPTION 'mutation_tool_names must be NULL when mutation_tier_enabled is false';\n    END IF;\n    \n    -- mutation_tier_enabled = true ⇒ mutation_tool_names NOT NULL\n    IF NEW.mutation_tier_enabled AND NEW.mutation_tool_names IS NULL THEN\n        RAISE EXCEPTION 'mutation_tool_names must not be NULL when mutation_tier_enabled is true';\n    END IF;\n    \n    -- base_tool_names가 유효한 JSON 배열\n    IF NOT (NEW.base_tool_names::jsonb @> '[]'::jsonb) THEN\n        RAISE EXCEPTION 'base_tool_names must be a valid JSON array';\n    END IF;\n    \n    -- mutation_tool_names가 유효한 JSON 배열 (있는 경우)\n    IF NEW.mutation_tool_names IS NOT NULL AND NOT (NEW.mutation_tool_names::jsonb @> '[]'::jsonb) THEN\n        RAISE EXCEPTION 'mutation_tool_names must be a valid JSON array';\n    END IF;\n    \n    RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER trg_validate_provider_capability\n    BEFORE INSERT OR UPDATE ON provider_capabilities\n    FOR EACH ROW\n    EXECUTE FUNCTION validate_provider_capability();\n```\n\n### 5.3 롤백 절차\n\n**단계 1: 마이그레이션 3-2 롤백**\n- register_errors 컬럼 복구 (original_register_errors에서).\n- register_context_failures 데이터 삭제.\n- register_failure_ids 컬럼 삭제.\n\n**단계 2: 마이그레이션 3-1 롤백**\n- register_context_failures, provider_capabilities 테이블 삭제.\n- 트리거 함수 삭제.\n\n**실행 순서:**\n1. 신규 애플리케이션 배포 중단.\n2. 롤백 SQL 스크립트 순차 실행.\n3. 이전 애플리케이션 버전 재배포.\n\n**파급 범위:**\n- register_errors를 쓰는 핸들러 (dispatcher, inbox) 영향.\n- provider capability 조회하는 L0009 로직 영향.\n- 기타 일반 쿼리는 영향 없음.\n\n### 5.4 실패 안전 설계\n\n**데이터 손실 방지:**\n- 마이그레이션 3-2에서 모든 register_errors 배열 원소가 register_context_failures로 변환되어 손실 없음.\n- 역변환 롤백도 register_context_failures에서 재구성 가능.\n- original_register_errors 백업으로 원본 JSON 보존.\n\n**동시성 보호:**\n- register_failure_ids 배열 업데이트는 원자적 `||` 연산자 사용.\n- correlation_id UNIQUE 인덱스로 중복 기록 방지.\n\n**외래키 보호:**\n- ON DELETE CASCADE로 run 삭제 시 관련 텔레메트리 자동 정리.\n- FK 위반 시 애플리케이션 레벨에서 catch 후 재시도 또는 로깅.\n\n---\n\n## DEFERRED\n\n없음.\n\n---\n\n## 체크리스트\n\n- [x] L0010 context-binding 네 축(action/project/group/doc) 텔레메트리를 모두 컬럼으로 갖춤.\n- [x] register_context_failures에 dispatcher/inbox 경계, axis_first_mismatch, axes_all_mismatches 구분 기록.\n- [x] binding_relaxed와 relaxed_axis로 review legacy token 완화 추적. 양방향 CHECK 제약 추가.\n- [x] provider_capabilities로 L0009 resolve_turn_tools의 tier 조건 메타데이터 저장. JSONB 타입 사용.\n- [x] ai_invoke_runs의 register_errors JSONB → register_context_failures 정규화 테이블 마이그레이션. original_register_errors 백업.\n- [x] 모든 FK에 ON DELETE 동작명시(CASCADE).\n- [x] 마이그레이션이 가역적 (롤백 스크립트 제공).\n- [x] 핫패스 쿼리에 적절한 인덱스 설계 (run_id/recorded_at, boundary/axis, provider/step 등).\n- [x] 무결성 불변식과 강제 수단 1:1 매칭. 트리거 함수 구현.\n- [x] 비즈니스 서술(L0010/L0009)과 DB 구조 섞임 없음.\n- [x] recorded_at COALESCE로 timestamp fallback 추가.\n- [x] base_tool_names, mutation_tool_names JSONB로 타입 변경.\n- [x] test_timeout_default_sec [1,600], api_call_timeout_max_sec [30,3600] 범위 CHECK 추가.\n- [x] mutation_tier_enabled와 mutation_tool_names 양방향 관계 CHECK 제약 추가.\n- [x] L0009 정의 status, reason, turn 필드는 original_register_errors 백업으로 보존.\n\n---\n\nnext_type: DB\nnext_type_detail: 데이터베이스\nproject: flowgate\nmodule: default\ngroup: 0492\ntitle: API 프로바이더 register 실패 텔레메트리·capability 메타데이터 DB설계\ntarget_id: B0001\n","doc_review_status":"rejected","rejection_reason":"## Automated review rejection\n\n수정본은 timeout·JSONB capability·일부 CHECK를 개선했지만, 핵심 CREATE TABLE은 아직 실행 불가하고 기존 register_errors의 구조화 진단을 실제로 매핑하지 않은 채 손실 없음이라고 선언합니다. 상위 L0010과 경계 enum도 불일치하며 텔레메트리 무결성/cardinality 설계가 실패 기록을 막을 수 있어 후속 구현 전에 모두 수정해야 합니다.\n\n- §3 마이그레이션 3-1 provider_capabilities CREATE TABLE: CREATE TABLE 내부의 `UNIQUE (provider_id, step_type) WHERE disabled_at IS NULL`은 PostgreSQL 문법상 여전히 실행 불가능합니다. 바로 아래 부분 UNIQUE INDEX가 이미 동일 제약을 제공하므로 테이블 제약 행을 제거하고, 문서 §2.2의 `UQ(provider_id, step_type, disabled_at IS NULL)` 표기도 실제 부분 인덱스 계약으로 통일해야 합니다.\n- §3 마이그레이션 3-2 register_errors 이관 및 §5.4 데이터 손실 방지: 원본 백업 UPDATE가 INSERT 뒤에 있어 잘못된 timestamp/boolean/배열 형식 하나만 있어도 이관이 먼저 중단되며, 이전 지적에서 요구한 안전한 timestamp 변환 정책이 구현되지 않았습니다. 또한 신규 notes에는 `error_obj->>'notes'`만 저장하므로 문서가 주장하는 status/reason/turn 포함이 거짓이고, 상위 L0010의 기존 키 `axis`, `axes`, `code`, `correlation_id`도 신규 명칭으로 매핑하지 않습니다. 누락 시 action/[action]을 채우는 기본값은 실제 실패 축을 조작합니다. 백업을 이관 전에 수행하고, 원본 payload를 행별 JSONB로 보존하거나 모든 계약 키를 명시적으로 매핑하며, TRY-safe 변환·검증 및 건수/내용 대조가 성공한 뒤에만 register_errors를 드롭해야 합니다.\n- §2.1 register_context_failures.boundary 및 §3.1 CHECK/DDL: 상위 L0010은 경계 값을 `register_dispatch` 또는 `inbox`로 정의하지만 이 문서는 `dispatcher` 또는 `inbox`만 허용합니다. 구현이 L0010의 구조화 로그 값을 그대로 기록하면 CHECK 위반으로 텔레메트리 저장 자체가 실패합니다. 명칭을 상위 계약과 일치시키고 이관 기본값·seed/검증도 같은 enum을 사용해야 합니다.\n- §2.1·§3.1 axes_all_mismatches 및 correlation_id 모델: `axes_all_mismatches`를 VARCHAR에 저장하고 트리거에서 cast하는 설계는 JSON 배열 여부, 허용 축(action/project/group/doc), 고정 순서, 중복 금지를 충분히 강제하지 못하며 잘못된 문자열은 trigger cast 예외로 쓰기를 중단합니다. JSONB 컬럼과 배열/원소/순서 CHECK 또는 검증 함수를 확정해야 합니다. 또한 한 요청이 dispatcher와 inbox 양 경계에서 관측될 수 있는 추적 ID인데 correlation_id를 단독 UNIQUE로 두면 동일 요청의 두 경계 이벤트를 함께 저장할 수 없습니다. 이벤트 고유 키를 별도로 두거나 `(correlation_id,boundary)` 등 실제 cardinality에 맞춰야 합니다.\n\nGET http://127.0.0.1:8089/flowgate/api/v1/document/flowgate.default.0492.0011-DB/reviews","rejection_history":[{"rejection_id":"rej_01M1CBPG6NFTWTF9","reason":"## Automated review rejection\n\n핵심 테이블 방향은 상위 설계와 부합하지만, 제시된 마이그레이션 SQL 두 곳이 실행 불가능하고 기존 register_errors의 진단 필드가 소실됩니다. 선언한 무결성 범위도 실제 DDL에서 강제되지 않아 후속 구현 전 수정이 필요합니다.\n\n- §3 마이그레이션 3-1 provider_capabilities CREATE TABLE/인덱스: PostgreSQL은 CREATE TABLE의 UNIQUE 테이블 제약 뒤에 WHERE 절을 허용하지 않으므로 `UNIQUE (provider_id, step_type, disabled_at) WHERE disabled_at IS NULL`은 구문 오류입니다. 활성 행 1개 제약은 `CREATE UNIQUE INDEX ... (provider_id, step_type) WHERE disabled_at IS NULL`로 작성해야 합니다. 또한 `disabled_at IS NULL DESC` 인덱스 키도 표현식을 괄호로 감싸는 실행 가능한 DDL로 확정해야 합니다.\n- §3 마이그레이션 3-2 기존 register_errors INSERT ... SELECT: INSERT 대상은 25개 컬럼인데 SELECT는 `recorded_at` 자리에 `(error_obj->>'recorded_at')::timestamp with time zone`와 `(air.updated_at AT TIME ZONE 'UTC')`를 각각 별도 식으로 내어 26개 값이 되므로 실행 즉시 실패합니다. 의도한 fallback을 `COALESCE(...)` 한 식으로 고치고, 잘못된 기존 timestamp가 전체 이관을 중단하지 않도록 변환 정책도 정해야 합니다.\n- §2.3·§3.2·§5.3~5.4 register_errors 데이터 이관 및 롤백: 상위 L0009가 기존 register_errors에 기록한다고 명시한 `status`, `reason`, `turn`을 신규 테이블 컬럼이나 notes에 옮기지 않으며 역변환 jsonb_build_object에도 포함하지 않습니다. 그런데 문서는 '모든 필드 변환/JSON 손실 없음'과 역변환 가능을 주장합니다. 임의의 기존 JSON 필드까지 보존하는 원본 payload 컬럼 또는 명시적 컬럼 매핑·검증·백업/드롭 게이트가 필요합니다.\n- §2.1~2.2 컬럼 제약, §3.1 DDL, §5.1 무결성 불변식: 문서가 불변식으로 선언한 제약이 기본 DDL에는 구현되지 않았습니다. timeout은 각각 [1,600], [30,3600]이어야 하나 DDL은 >0만 검사하고, binding_relaxed=false일 때 relaxed_axis가 NULL이어야 한다는 양방향 조건도 없으며, mutation_tier_enabled=false와 mutation_tool_names의 관계 및 axes JSON/첫 축 검증은 '선택적' 트리거에만 의존합니다. 특히 TEXT를 조회 시 jsonb로 cast하므로 잘못된 값 하나가 읽기 경로를 실패시킬 수 있습니다. JSONB 타입과 CHECK를 포함한 필수 DB 제약으로 실행 DDL을 체크리스트와 일치시켜야 합니다.\n\nGET http://127.0.0.1:8089/flowgate/api/v1/document/flowgate.default.0492.0011-DB/reviews","rejected_at":"2026-09-01T01:51:01+09:00","rejected_by":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","ai_response":"§3 마이그레이션 3-1: PostgreSQL UNIQUE 제약 문법 수정. CREATE UNIQUE INDEX로 변경. §3 마이그레이션 3-2: INSERT ... SELECT 컬럼 개수 불일치 수정. recorded_at을 COALESCE로 통합. §2.2-2.3: base_tool_names, mutation_tool_names JSONB로 변경. original_register_errors 백업. §2.1-2.2: timeout 범위 CHECK 제약 추가. 양방향 조건. §5.1-5.2: 트리거 함수 구현. 모든 제약이 DDL에 명시되고 트리거로 강제됨.","responded_at":"2026-09-01T01:55:23+09:00","response_recorded_by":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","response_revision_no":1,"review_id":505},{"rejection_id":"rej_01M1CC3RM4NCW22R","reason":"## Automated review rejection\n\n수정본은 timeout·JSONB capability·일부 CHECK를 개선했지만, 핵심 CREATE TABLE은 아직 실행 불가하고 기존 register_errors의 구조화 진단을 실제로 매핑하지 않은 채 손실 없음이라고 선언합니다. 상위 L0010과 경계 enum도 불일치하며 텔레메트리 무결성/cardinality 설계가 실패 기록을 막을 수 있어 후속 구현 전에 모두 수정해야 합니다.\n\n- §3 마이그레이션 3-1 provider_capabilities CREATE TABLE: CREATE TABLE 내부의 `UNIQUE (provider_id, step_type) WHERE disabled_at IS NULL`은 PostgreSQL 문법상 여전히 실행 불가능합니다. 바로 아래 부분 UNIQUE INDEX가 이미 동일 제약을 제공하므로 테이블 제약 행을 제거하고, 문서 §2.2의 `UQ(provider_id, step_type, disabled_at IS NULL)` 표기도 실제 부분 인덱스 계약으로 통일해야 합니다.\n- §3 마이그레이션 3-2 register_errors 이관 및 §5.4 데이터 손실 방지: 원본 백업 UPDATE가 INSERT 뒤에 있어 잘못된 timestamp/boolean/배열 형식 하나만 있어도 이관이 먼저 중단되며, 이전 지적에서 요구한 안전한 timestamp 변환 정책이 구현되지 않았습니다. 또한 신규 notes에는 `error_obj->>'notes'`만 저장하므로 문서가 주장하는 status/reason/turn 포함이 거짓이고, 상위 L0010의 기존 키 `axis`, `axes`, `code`, `correlation_id`도 신규 명칭으로 매핑하지 않습니다. 누락 시 action/[action]을 채우는 기본값은 실제 실패 축을 조작합니다. 백업을 이관 전에 수행하고, 원본 payload를 행별 JSONB로 보존하거나 모든 계약 키를 명시적으로 매핑하며, TRY-safe 변환·검증 및 건수/내용 대조가 성공한 뒤에만 register_errors를 드롭해야 합니다.\n- §2.1 register_context_failures.boundary 및 §3.1 CHECK/DDL: 상위 L0010은 경계 값을 `register_dispatch` 또는 `inbox`로 정의하지만 이 문서는 `dispatcher` 또는 `inbox`만 허용합니다. 구현이 L0010의 구조화 로그 값을 그대로 기록하면 CHECK 위반으로 텔레메트리 저장 자체가 실패합니다. 명칭을 상위 계약과 일치시키고 이관 기본값·seed/검증도 같은 enum을 사용해야 합니다.\n- §2.1·§3.1 axes_all_mismatches 및 correlation_id 모델: `axes_all_mismatches`를 VARCHAR에 저장하고 트리거에서 cast하는 설계는 JSON 배열 여부, 허용 축(action/project/group/doc), 고정 순서, 중복 금지를 충분히 강제하지 못하며 잘못된 문자열은 trigger cast 예외로 쓰기를 중단합니다. JSONB 컬럼과 배열/원소/순서 CHECK 또는 검증 함수를 확정해야 합니다. 또한 한 요청이 dispatcher와 inbox 양 경계에서 관측될 수 있는 추적 ID인데 correlation_id를 단독 UNIQUE로 두면 동일 요청의 두 경계 이벤트를 함께 저장할 수 없습니다. 이벤트 고유 키를 별도로 두거나 `(correlation_id,boundary)` 등 실제 cardinality에 맞춰야 합니다.\n\nGET http://127.0.0.1:8089/flowgate/api/v1/document/flowgate.default.0492.0011-DB/reviews","rejected_at":"2026-09-01T01:58:16+09:00","rejected_by":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","ai_response":null,"responded_at":null,"response_recorded_by":null,"response_revision_no":null,"review_id":506}],"created_at":"2026-09-01T01:48:04+09:00","updated_at":"2026-09-01T01:58:16+09:00","ai_review":{"id":506,"revision_no":1,"reviewer_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","verdict":"issues","finding_count":4,"findings":[{"locus":"§3 마이그레이션 3-1 provider_capabilities CREATE TABLE","note":"CREATE TABLE 내부의 `UNIQUE (provider_id, step_type) WHERE disabled_at IS NULL`은 PostgreSQL 문법상 여전히 실행 불가능합니다. 바로 아래 부분 UNIQUE INDEX가 이미 동일 제약을 제공하므로 테이블 제약 행을 제거하고, 문서 §2.2의 `UQ(provider_id, step_type, disabled_at IS NULL)` 표기도 실제 부분 인덱스 계약으로 통일해야 합니다."},{"locus":"§3 마이그레이션 3-2 register_errors 이관 및 §5.4 데이터 손실 방지","note":"원본 백업 UPDATE가 INSERT 뒤에 있어 잘못된 timestamp/boolean/배열 형식 하나만 있어도 이관이 먼저 중단되며, 이전 지적에서 요구한 안전한 timestamp 변환 정책이 구현되지 않았습니다. 또한 신규 notes에는 `error_obj->>'notes'`만 저장하므로 문서가 주장하는 status/reason/turn 포함이 거짓이고, 상위 L0010의 기존 키 `axis`, `axes`, `code`, `correlation_id`도 신규 명칭으로 매핑하지 않습니다. 누락 시 action/[action]을 채우는 기본값은 실제 실패 축을 조작합니다. 백업을 이관 전에 수행하고, 원본 payload를 행별 JSONB로 보존하거나 모든 계약 키를 명시적으로 매핑하며, TRY-safe 변환·검증 및 건수/내용 대조가 성공한 뒤에만 register_errors를 드롭해야 합니다."},{"locus":"§2.1 register_context_failures.boundary 및 §3.1 CHECK/DDL","note":"상위 L0010은 경계 값을 `register_dispatch` 또는 `inbox`로 정의하지만 이 문서는 `dispatcher` 또는 `inbox`만 허용합니다. 구현이 L0010의 구조화 로그 값을 그대로 기록하면 CHECK 위반으로 텔레메트리 저장 자체가 실패합니다. 명칭을 상위 계약과 일치시키고 이관 기본값·seed/검증도 같은 enum을 사용해야 합니다."},{"locus":"§2.1·§3.1 axes_all_mismatches 및 correlation_id 모델","note":"`axes_all_mismatches`를 VARCHAR에 저장하고 트리거에서 cast하는 설계는 JSON 배열 여부, 허용 축(action/project/group/doc), 고정 순서, 중복 금지를 충분히 강제하지 못하며 잘못된 문자열은 trigger cast 예외로 쓰기를 중단합니다. JSONB 컬럼과 배열/원소/순서 CHECK 또는 검증 함수를 확정해야 합니다. 또한 한 요청이 dispatcher와 inbox 양 경계에서 관측될 수 있는 추적 ID인데 correlation_id를 단독 UNIQUE로 두면 동일 요청의 두 경계 이벤트를 함께 저장할 수 없습니다. 이벤트 고유 키를 별도로 두거나 `(correlation_id,boundary)` 등 실제 cardinality에 맞춰야 합니다."}],"comment":"수정본은 timeout·JSONB capability·일부 CHECK를 개선했지만, 핵심 CREATE TABLE은 아직 실행 불가하고 기존 register_errors의 구조화 진단을 실제로 매핑하지 않은 채 손실 없음이라고 선언합니다. 상위 L0010과 경계 enum도 불일치하며 텔레메트리 무결성/cardinality 설계가 실패 기록을 막을 수 있어 후속 구현 전에 모두 수정해야 합니다.","reviewed_at":"2026-09-01T01:58:07+09:00","created_at":"2026-09-01T01:58:07+09:00"},"ai_review_history":[{"id":506,"revision_no":1,"reviewer_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","verdict":"issues","finding_count":4,"findings":[{"locus":"§3 마이그레이션 3-1 provider_capabilities CREATE TABLE","note":"CREATE TABLE 내부의 `UNIQUE (provider_id, step_type) WHERE disabled_at IS NULL`은 PostgreSQL 문법상 여전히 실행 불가능합니다. 바로 아래 부분 UNIQUE INDEX가 이미 동일 제약을 제공하므로 테이블 제약 행을 제거하고, 문서 §2.2의 `UQ(provider_id, step_type, disabled_at IS NULL)` 표기도 실제 부분 인덱스 계약으로 통일해야 합니다."},{"locus":"§3 마이그레이션 3-2 register_errors 이관 및 §5.4 데이터 손실 방지","note":"원본 백업 UPDATE가 INSERT 뒤에 있어 잘못된 timestamp/boolean/배열 형식 하나만 있어도 이관이 먼저 중단되며, 이전 지적에서 요구한 안전한 timestamp 변환 정책이 구현되지 않았습니다. 또한 신규 notes에는 `error_obj->>'notes'`만 저장하므로 문서가 주장하는 status/reason/turn 포함이 거짓이고, 상위 L0010의 기존 키 `axis`, `axes`, `code`, `correlation_id`도 신규 명칭으로 매핑하지 않습니다. 누락 시 action/[action]을 채우는 기본값은 실제 실패 축을 조작합니다. 백업을 이관 전에 수행하고, 원본 payload를 행별 JSONB로 보존하거나 모든 계약 키를 명시적으로 매핑하며, TRY-safe 변환·검증 및 건수/내용 대조가 성공한 뒤에만 register_errors를 드롭해야 합니다."},{"locus":"§2.1 register_context_failures.boundary 및 §3.1 CHECK/DDL","note":"상위 L0010은 경계 값을 `register_dispatch` 또는 `inbox`로 정의하지만 이 문서는 `dispatcher` 또는 `inbox`만 허용합니다. 구현이 L0010의 구조화 로그 값을 그대로 기록하면 CHECK 위반으로 텔레메트리 저장 자체가 실패합니다. 명칭을 상위 계약과 일치시키고 이관 기본값·seed/검증도 같은 enum을 사용해야 합니다."},{"locus":"§2.1·§3.1 axes_all_mismatches 및 correlation_id 모델","note":"`axes_all_mismatches`를 VARCHAR에 저장하고 트리거에서 cast하는 설계는 JSON 배열 여부, 허용 축(action/project/group/doc), 고정 순서, 중복 금지를 충분히 강제하지 못하며 잘못된 문자열은 trigger cast 예외로 쓰기를 중단합니다. JSONB 컬럼과 배열/원소/순서 CHECK 또는 검증 함수를 확정해야 합니다. 또한 한 요청이 dispatcher와 inbox 양 경계에서 관측될 수 있는 추적 ID인데 correlation_id를 단독 UNIQUE로 두면 동일 요청의 두 경계 이벤트를 함께 저장할 수 없습니다. 이벤트 고유 키를 별도로 두거나 `(correlation_id,boundary)` 등 실제 cardinality에 맞춰야 합니다."}],"comment":"수정본은 timeout·JSONB capability·일부 CHECK를 개선했지만, 핵심 CREATE TABLE은 아직 실행 불가하고 기존 register_errors의 구조화 진단을 실제로 매핑하지 않은 채 손실 없음이라고 선언합니다. 상위 L0010과 경계 enum도 불일치하며 텔레메트리 무결성/cardinality 설계가 실패 기록을 막을 수 있어 후속 구현 전에 모두 수정해야 합니다.","reviewed_at":"2026-09-01T01:58:07+09:00","created_at":"2026-09-01T01:58:07+09:00"},{"id":505,"revision_no":0,"reviewer_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","verdict":"issues","finding_count":4,"findings":[{"locus":"§3 마이그레이션 3-1 provider_capabilities CREATE TABLE/인덱스","note":"PostgreSQL은 CREATE TABLE의 UNIQUE 테이블 제약 뒤에 WHERE 절을 허용하지 않으므로 `UNIQUE (provider_id, step_type, disabled_at) WHERE disabled_at IS NULL`은 구문 오류입니다. 활성 행 1개 제약은 `CREATE UNIQUE INDEX ... (provider_id, step_type) WHERE disabled_at IS NULL`로 작성해야 합니다. 또한 `disabled_at IS NULL DESC` 인덱스 키도 표현식을 괄호로 감싸는 실행 가능한 DDL로 확정해야 합니다."},{"locus":"§3 마이그레이션 3-2 기존 register_errors INSERT ... SELECT","note":"INSERT 대상은 25개 컬럼인데 SELECT는 `recorded_at` 자리에 `(error_obj->>'recorded_at')::timestamp with time zone`와 `(air.updated_at AT TIME ZONE 'UTC')`를 각각 별도 식으로 내어 26개 값이 되므로 실행 즉시 실패합니다. 의도한 fallback을 `COALESCE(...)` 한 식으로 고치고, 잘못된 기존 timestamp가 전체 이관을 중단하지 않도록 변환 정책도 정해야 합니다."},{"locus":"§2.3·§3.2·§5.3~5.4 register_errors 데이터 이관 및 롤백","note":"상위 L0009가 기존 register_errors에 기록한다고 명시한 `status`, `reason`, `turn`을 신규 테이블 컬럼이나 notes에 옮기지 않으며 역변환 jsonb_build_object에도 포함하지 않습니다. 그런데 문서는 '모든 필드 변환/JSON 손실 없음'과 역변환 가능을 주장합니다. 임의의 기존 JSON 필드까지 보존하는 원본 payload 컬럼 또는 명시적 컬럼 매핑·검증·백업/드롭 게이트가 필요합니다."},{"locus":"§2.1~2.2 컬럼 제약, §3.1 DDL, §5.1 무결성 불변식","note":"문서가 불변식으로 선언한 제약이 기본 DDL에는 구현되지 않았습니다. timeout은 각각 [1,600], [30,3600]이어야 하나 DDL은 >0만 검사하고, binding_relaxed=false일 때 relaxed_axis가 NULL이어야 한다는 양방향 조건도 없으며, mutation_tier_enabled=false와 mutation_tool_names의 관계 및 axes JSON/첫 축 검증은 '선택적' 트리거에만 의존합니다. 특히 TEXT를 조회 시 jsonb로 cast하므로 잘못된 값 하나가 읽기 경로를 실패시킬 수 있습니다. JSONB 타입과 CHECK를 포함한 필수 DB 제약으로 실행 DDL을 체크리스트와 일치시켜야 합니다."}],"comment":"핵심 테이블 방향은 상위 설계와 부합하지만, 제시된 마이그레이션 SQL 두 곳이 실행 불가능하고 기존 register_errors의 진단 필드가 소실됩니다. 선언한 무결성 범위도 실제 DDL에서 강제되지 않아 후속 구현 전 수정이 필요합니다.","reviewed_at":"2026-09-01T01:50:53+09:00","created_at":"2026-09-01T01:50:53+09:00"}],"test_run":null,"test_run_history":[]}