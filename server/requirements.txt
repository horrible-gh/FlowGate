uvicorn[standard]==0.34.0
fastapi==0.115.8
passlib==1.7.4
pyjwt==2.10.1
pydantic_settings==2.7.1
bcrypt==4.3.0
python-multipart==0.0.20
LogAssist==1.1.4
redis==5.2.1
PyMySQL==1.1.1
pycryptodome==3.23.0
sqloader==0.2.17
slowapi==0.1.9
email-validator==2.3.0
psycopg2-binary==2.9.12
pytest==8.3.4
pytest-asyncio==0.25.2
# 0394 T0004 (NR0003 §10.3): the suite is 3,200+ cases on one process and the time is
# spread thin, so tiers alone cannot fix it — parallel execution is the only lever that
# shortens a FULL run without deleting a test. Use `-n auto --dist loadfile`; loadfile is
# required, because module/session-scoped DB fixtures break if one file is split across
# workers. See TESTING.md.
pytest-xdist==3.6.1
httpx==0.28.1
requests==2.32.3
auth2fa==0.1.4
pyotp==2.9.0
python-jose==3.0.0
python-dotenv==0.21.0
jinja2>=3.1.0
Unidecode>=1.3.0
