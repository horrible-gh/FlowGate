{"ok":true,"doc_id":"flowgate.default.0492.0009-L","type":"L","title":"API 프로바이더 server-mediated 도구 디스패치 로직설계","status":"open","revision_no":1,"owner_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","triggered_by":"flowgate.default.0492.0001-B","group_id":"flowgate.default.0492","project":"flowgate","module":"default","branch":"main","stored_path":"documents/flowgate/main/default/0492/0009-L_document.md","content":"# API 프로바이더 server-mediated 도구 디스패치 로직설계 (L)\n\n> 단계: 로직설계(L) — WP0004 L#2\n> 참조: D0005(§2 도구 레지스트리·실행 경계 / §3 도구 노출·배정 시점), P0006, P0007, L0008, NR0003 R3·R4\n\n---\n\n## 목적\n\n`ai_invoke_service._api_execute`가 문서 완료용 단일 도구만 노출·처리하는 구조를 action scope와 현재 workflow 단계에 따른 도구셋 선택 및 이름 기반 디스패치로 확장한다. 소스 도구의 경로 검증, 모든 소스 변경과 테스트의 run 소속 그룹 worktree 고정, allowlist 테스트 실행 경계, turn 계속·완료 판정 및 구현 검증 범위를 확정한다.\n\n## 1. 파라미터 정의\n\n| 파라미터 | 값 | 단위 | 설명 | 출처 |\n|---|---:|---|---|---|\n| `exclusive_scopes` | `workflow_decide`, `resolve_conflict`, `chat` | scope 3개 | 기존 단일 강제 도구 경로를 유지하는 범위 | 현행 `_api_execute`, P0007 |\n| `document_scopes` | `new`, `edit`, `review`, `test_run` | scope 4개 | server-mediated 도구셋을 노출하는 문서 처리 범위 | P0007 |\n| `base_tool_names` | `read_document`, `create_question`, `register_document` | 도구 3개 | 모든 document scope turn에 노출 | P0007 |\n| `mutation_step_types` | `T`, `TR` | 타입 2개 | 확장 tier 노출 대상 | L0008, P0007 |\n| `mutation_tool_names` | `read_source_file`, `search_source`, `patch_source_file`, `write_source_file`, `run_test` | 도구 5개 | T/TR turn에만 추가 | P0007 |\n| `completion_tool_name` | `register_document` | 도구 1개 | 성공 시 완료 카운터 또는 scoped oracle을 진전시키는 유일한 도구 | P0007 |\n| `source_op_map` | read→`read`, search→`grep`, patch→`patch`, write→`write` | 매핑 4개 | provider 도구명을 기존 remote op으로 변환 | P0007 |\n| `source_ref` | `null` | 값 1개 | API 모델이 커밋 tree를 선택하지 못하게 하고 현재 worktree만 사용 | P0007 |\n| `api_call_timeout_max` | 600 | 초 | 한 provider API 호출 상한. run 잔여시간과 작은 값을 사용 | 현행 `API_CALL_MAX_TIMEOUT_SEC` |\n| `tool_http_timeout` | 120 | 초 | 문서·질의·등록 내부 HTTP 호출 상한 | 현행 proxy 호출 |\n| `test_timeout_default` | 300 | 초 | 등록 명령에 별도 제한이 없을 때 한 테스트 프로세스 상한 | 설계 결정 |\n\n| `test_output_max_bytes` | 1,048,576 | 바이트/스트림 | stdout, stderr 각각 보존하는 최대 크기 | 설계 결정 |\n| `test_result_max_chars` | 16,000 | 문자 | provider 대화에 되돌리는 정규화 결과 상한 | turn context 보호 결정 |\n| `test_kill_grace` | 5 | 초 | timeout/cancel 후 프로세스 트리 종료와 회수 대기 | 현행 프로세스 회수 관례 |\n| `max_tools_per_turn` | 8 | 도구 | T/TR document scope의 최대 노출 수 | P0007 도구 수 |\n\n`API_MAX_TURNS_PER_DOC`, run의 전체 deadline, no-output 재시도 규칙은 현행 값을 유지한다. 중간 도구 호출 횟수를 별도 무제한 루프로 만들지 않고 기존 turn 상한 안에서 함께 센다.\n\n## 2. 알고리즘 / 처리 로직\n\n### 2.1 turn 도구셋 결정\n\n도구 정의는 `name`, `description`, `schema`, `handler_kind`, `completion`을 갖는 불변 registry로 관리한다. `register_document.schema`만 run의 `action_scope`로 new/edit/review/test_run 변형을 선택한다. provider별 payload 변환기는 이 목록을 Anthropic `tools[]` 또는 OpenAI function `tools[]`로 직렬화하며, 응답 파서는 목록에 포함된 어느 이름의 호출도 반환할 수 있어야 한다.\n\n```text\nfunction resolve_turn_tools(run):\n    scope = normalize(run.action_scope)\n\n    if scope == workflow_decide:\n        return tools=[decide_workflow], choice=force(decide_workflow)\n    elif scope == resolve_conflict:\n        return tools=[resolve_git_conflict], choice=force(resolve_git_conflict)\n    elif scope == chat:\n        return tools=[send_chat_reply], choice=force(send_chat_reply)\n    elif scope is not in document_scopes:\n        return invalid_scope\n\n    tools = base_tool_names\n    step_type = resolve_bound_step_type(run)\n    if step_type is in mutation_step_types:\n        tools = tools + mutation_tool_names\n    else:\n        tools = tools\n\n    replace register_document schema with schema_for(scope)\n    return tools preserving registry order, choice=auto\n```\n\n`resolve_bound_step_type`은 모델 입력이나 prompt 문자열을 읽지 않는다. run의 `doc_ref`가 속한 workflow sequence의 유효 head를 조회하고, self-scoped 문서이면 자신의 type을 사용한다. 조회 실패·head 부재는 확장 tier를 숨기는 fail-closed 결과이며 기본 tier는 유지한다. T/TR인데 mutation tier 준비 조건이 충족되지 않으면 L0008의 provider capability gate가 run 생성 전에 차단해야 한다. 이미 시작된 run에서 불일치가 발견되면 mutation 도구를 부분 노출하지 않고 내부 원인 라벨 `toolset_unavailable`로 종료한다. provider tool_result에는 HTTP 503, `error.code=unavailable`, `error.details.reason=toolset_unavailable`로 정규화한다.\n\n### 2.2 provider 호출과 tool_call 수용\n\n```text\nfunction request_provider(turn_tools, conversation, remaining):\n    timeout = min(remaining, api_call_timeout_max)\n    send every turn_tool definition in one provider request\n    send forced choice only for exclusive_scopes; otherwise send auto choice\n    parse assistant text and zero or more tool calls in provider order\n    return assistant message plus normalized calls\n```\n\n한 응답에 복수 tool call이 오면 순서대로 처리하되, 첫 완료 도구가 성공한 뒤의 호출은 실행하지 않는다. mutation 호출 다음 register 호출처럼 앞 호출 결과를 보지 않고 미리 만든 복수 호출은 허용하되 각 호출은 독립 검증한다. 한 호출이 실패하면 그 실패 결과를 붙이고 같은 응답의 후속 호출은 실행하지 않아 모델이 오류를 보고 다음 turn에 수정하게 한다. 알 수 없는 이름 또는 현재 turn에 미노출된 이름은 내부 원인 라벨 `invalid_tool_call`로 분류하고 provider tool_result에는 HTTP 422, `error.code=invalid_request`, `error.details.reason=invalid_tool_call`로 정규화하며 핸들러를 호출하지 않는다. 비객체 input도 `error.code=invalid_request`로 반환하되 `error.details.reason=schema_validation_failed`로 구분한다.\n\ntool call이 없으면 기존 nudge 횟수와 turn 상한을 유지하되 문구는 단일 이름 강제가 아니라 “노출된 도구 중 필요한 작업을 호출하고, 완료 시 `register_document`를 호출하라”로 바꾼다. 중간 도구 성공은 `registered`를 올리지 않으며 `tool_call_misses`도 올리지 않는다.\n\n### 2.3 이름 기반 디스패치\n\n```text\nfunction dispatch_tool(run, token, call, exposed_tools):\n    if call.name is not in exposed_tools:\n        return failure(code=invalid_request, details.reason=invalid_tool_call)\n    if call.input is not an object:\n        return failure(invalid_request)\n\n    validate call.input against the exact exposed schema\n\n    switch call.name:\n        read_source_file:\n            return dispatch_source(run, token, read, call.input)\n        search_source:\n            return dispatch_source(run, token, grep, call.input)\n        patch_source_file:\n            return dispatch_source(run, token, patch, call.input)\n        write_source_file:\n            return dispatch_source(run, token, write, call.input)\n        run_test:\n            return dispatch_test(run, token, call.input)\n        read_document:\n            return dispatch_document_read(run, token, call.input)\n        create_question:\n            return dispatch_question(run, token, call.input)\n        register_document:\n            return dispatch_completion_by_scope(run, token, call.input)\n        default:\n            return failure(code=invalid_request, details.reason=invalid_tool_call)\n```\n\n모든 결과는 P0007의 `{ok, op, ...}` 또는 `{ok:false, op, error:{code,message,details?}}`로 정규화한 뒤 provider별 tool-result message로 변환한다. 내부 예외·연결 실패는 원문 stack이나 secret을 노출하지 않고 `unavailable`로 정규화한다. 완료 도구 실패는 기존처럼 `register_errors`에 status/reason/turn을 기록하고 루프를 계속한다.\n\n### 2.4 run 고정 실행 컨텍스트\n\ndispatcher는 모델 input으로 grant, project, module, group, doc_ref, action_scope, token, source root를 받지 않는다. 다음 순서로 매 호출의 고정 컨텍스트를 새로 만든다.\n\n```text\nfunction bind_run_context(run, current_token):\n    token_row = lookup current_token\n    require token_row exists, active, unexpired\n    require token_row.token_id == run.current_token_id\n    require token_row.project == run.project_id\n    require token_row.group == run.group_id\n    require token_row.doc_ref == run.doc_ref or is server-issued continuation token\n    require token_row.action_scope == run.action_scope\n\n    grant = derive scopes from token_row and bound workflow step\n    root = resolve exact group worktree(run.project_id, run.group_id)\n    require root identity matches git worktree ledger and run.source_root\n    return immutable context(token_row, grant, root, run fields)\n```\n\n`run.source_root`가 없거나 디렉터리가 아니거나 base checkout/scratch/다른 group worktree를 가리키면 mutation과 run_test는 HTTP 409, `error.code=conflict`, `error.details.reason=group_worktree_unavailable`로 fail-closed한다. `group_worktree_unavailable`은 top-level 오류 코드가 아니라 내부 원인 라벨이다. read/search도 API 도구셋에서는 같은 그룹 worktree를 보도록 고정한다. 기존 일반 remote read API가 제공하는 base checkout fallback은 이 dispatcher에서 사용하지 않는다. 이유는 한 turn 안의 read→patch→test가 서로 다른 tree를 보게 되는 것을 막기 위해서다. merge conflict session 전용 root도 `resolve_conflict` 단일 경로에서만 사용하며 document scope 소스 도구에는 섞지 않는다.\n\ncontinuation에서 token이 바뀌면 run의 `current_token_id`와 action scope를 서버가 먼저 갱신한 뒤 다음 turn을 연다. 이전 token으로 이미 만들어진 지연 호출은 owner mismatch로 거부한다.\n\n### 2.5 소스 경로 검증과 remote op 위임\n\n경로 검증은 dispatcher의 사전 검증과 `remote_tool_service`의 기존 검증을 모두 통과해야 한다. 어느 한쪽도 다른 쪽을 대체하지 않는다.\n\n```text\nfunction validate_source_input(op, input, bound_root):\n    for every present path-bearing field:\n        require value is a string\n        require value is root-relative and non-absolute\n        require no empty segment, dot segment, parent segment, drive, UNC, NUL\n        require normalized lexical path remains relative\n\n    candidate = resolve bound_root joined with path\n    require candidate is within resolved bound_root\n    require every existing parent does not escape through symlink/reparse point\n    apply op-specific required/type/size/encoding validation\n```\n\n`search_source.glob`도 path-bearing 값으로 동일 검증한다. read/search에는 `ref=null`을 서버가 주입한다. patch/write는 `_resolve_root_for_mutation`의 결과가 사전에 고정한 root와 동일한지 다시 비교하고, worktree 부재·열린 merge 충돌·provision 실패 시 base checkout으로 fallback하지 않는다. 검증 후 기존 `remote_tool_service.handle`에 op, 정제된 body, run token에서 파생한 grant를 전달한다. 결과의 path는 root 상대경로만 반환하며 절대 host 경로는 모델에게 노출하지 않는다.\n\n### 2.6 run_test 실행 경계\n\n`run_test` 입력의 `command`는 자유 shell 문자열이 아니라 현재 project의 활성 `project_test_commands` 행과 정규화 후 정확히 일치해야 한다. 현재 host OS와 다른 `verified_os` 행은 실행 후보가 아니다. 입력은 P0007 스키마의 `command` 하나만 받으며(`additionalProperties=false`), timeout/cwd/env/shell/executable을 모델 입력으로 받지 않는다.\n\n```text\nfunction dispatch_test(run, token, input):\n    context = bind_run_context(run, token)\n    require bound step type is T or TR\n    require grant includes test permission\n    command = normalize whitespace(input.command)\n    row = find active project command with exact normalized command\n    require row exists and row.verified_os is absent or equals current host OS\n\n    require run.remaining_seconds > 0\n    timeout = min(test_timeout_default, run.remaining_seconds)\n\n    launch exactly row.command through host's registered shell\n    cwd = context.group_worktree\n    environment = server-built minimal test environment\n    do not accept model-provided env, cwd, redirection target, or shell options\n    capture stdout and stderr separately up to test_output_max_bytes each\n    on cancel or timeout, terminate the whole process tree and wait test_kill_grace\n    return {ok, command, exit_code, duration_ms, stdout, stderr, truncated}\n```\n\n명령 문자열 내부의 shell 문법은 allowlist 등록 시 승인된 동일 문자열의 일부이므로 다시 토큰화하거나 재작성하지 않는다. 모델이 명령 일부, 추가 인자, `cd ... &&`, 환경변수 prefix를 붙이면 정확 일치가 깨져 422 `invalid_request`, `details.reason=not_verified`다. 테스트는 동기적으로 종료·timeout·cancel까지 회수한 뒤 tool_result를 반환한다. background process를 남기거나 “started”만 반환하지 않는다. exit code 0은 `exit_code=0`으로, 비영 종료는 해당 `exit_code`로 기록한다. timeout·cancel도 프로세스 회수 후 P0007의 고정 결과 필드 `{ok, command, exit_code, duration_ms, stdout, stderr, truncated}`만 사용하며 `passed`나 `timed_out` 필드를 추가하지 않는다. 이 실행 결과는 transport/검증 오류와 구분해 `ok=true`로 반환한다. 실패 결과도 다음 turn에 전달되어 모델이 수정 후 다시 테스트할 수 있다.\n\n출력은 각 스트림의 뒤쪽을 우선 보존하고 UTF-8 대체 디코딩한다. 전체 정규화 JSON이 `test_result_max_chars`를 넘으면 stdout/stderr를 균등하게 더 줄이고 `truncated=true`를 표시한다. token, API key, 전체 환경변수, 절대 cwd는 결과와 로그에서 제외한다.\n\n### 2.7 완료와 turn 전이\n\n```text\nfor each provider turn:\n    tools = resolve_turn_tools(run)\n    response = request_provider(tools)\n\n    if response has no tool call:\n        nudge or stop by existing miss limit\n        continue\n\n    for call in response calls preserving order:\n        result = dispatch_tool(run, current_token, call, tools)\n        append result to conversation\n\n        if result failed:\n            record diagnostics when completion call\n            break to next provider turn\n\n        if call.name != completion_tool_name:\n            continue\n\n        apply next_token and next_mention only from server completion response\n        increment registered only for new document completion\n        evaluate scoped completion oracle for edit/review/test_run\n        if single is complete or docs target reached or no next token:\n            finish loop\n        break to next provider turn\n```\n\n`read_document`, `create_question`, 소스 도구, `run_test`는 문서를 완료하지 않는다. 특히 `create_question` 성공 후 pending question이 생기면 모델이 억지로 완료 문서를 만들지 않도록 현재 turn을 끝내고 기존 `question_pending` 정지 판정에 맡긴다. `register_document(test_run)`은 TS 원격 실행 트리거이고 이 절의 allowlist `run_test`와 다르다. 전자는 action scope의 완료 동작, 후자는 T/TR 구현 중 선택적으로 실행하는 동기 검증 도구다.\n\n## 3. 상태 전이\n\n| 상태 | 설명 | 진입 조건 |\n|---|---|---|\n| `selecting` | 현재 turn 도구셋 계산 중 | turn 시작 |\n| `awaiting_call` | provider 응답 대기 | 도구셋·choice 확정 |\n| `dispatching` | 노출된 중간/완료 도구 실행 중 | 유효 tool_call 수신 |\n| `continuing` | 중간 결과 또는 복구 가능한 실패를 대화에 추가 | 비완료 호출 또는 호출 실패 |\n| `completed` | 완료 동작 성공 | register_document와 scope oracle 성공 |\n| `waiting_question` | 사람 답변 대기 | create_question 성공 및 pending 존재 |\n| `failed` | 안전하게 실행할 수 없음 | scope/toolset/context binding 불일치, deadline/cancel |\n\n```text\n[selecting] --유효 도구셋--> [awaiting_call]\n[awaiting_call] --중간 호출--> [dispatching] --결과--> [continuing] --> [awaiting_call]\n[awaiting_call] --완료 호출--> [dispatching] --성공--> [completed]\n[dispatching] --복구 가능 실패--> [continuing]\n[dispatching] --질의 생성--> [waiting_question]\n[selecting|dispatching] --고정 컨텍스트 실패/취소/기한--> [failed]\n```\n\n## 4. 결정 트리 / 분기 조건\n\n```text\nif action_scope is workflow_decide, resolve_conflict, or chat:\n    → 기존 단일 강제 도구 경로\nelif action_scope is not a document scope:\n    → fail-closed invalid_scope\nelse:\n    expose base tier with auto choice\n    if bound step type is T or TR:\n        if group worktree and mutation/test registry are ready:\n            → expose all eight tools\n        else:\n            → unavailable(details.reason=toolset_unavailable); 부분 tier로 실행하지 않음\n    else:\n        → expose base three tools only\n\non tool_call:\n    if name was not exposed or input violates schema:\n        → invalid_request(details.reason=invalid_tool_call 또는 schema_validation_failed), no side effect\n    elif source tool:\n        if run/group/worktree binding or path containment fails:\n            → reject without fallback\n        else:\n            → mapped remote op and continue turn\n    elif run_test:\n        if command is not exact active host-compatible allowlist entry:\n            → invalid_request(not_verified)\n        else:\n            → synchronous bounded execution and continue turn\n    elif register_document:\n        → action_scope-specific completion handler\n    elif read_document or create_question:\n        → token/run-bound service call\n    else:\n        → default reject\n```\n\n## 5. 경계 조건 및 예외 처리\n\n| 상황 | 발생 조건 | 처리 방법 | 비고 |\n|---|---|---|---|\n| workflow head 조회 실패 | sequence/head 저장소 오류 | 기본 tier만 노출하지 않고 T/TR 가능성을 배제할 수 없으면 HTTP 503, `unavailable`, reason `toolset_unavailable` | fail-closed |\n| 미노출 도구 호출 | 모델이 현재 tier 밖 이름 호출 | side effect 없이 HTTP 422, `invalid_request`, reason `invalid_tool_call` | 이름 prefix/대소문자 보정 없음 |\n| 복수 완료 호출 | 한 응답에 register가 둘 이상 | 첫 성공까지만 실행, 나머지 폐기 | 중복 문서 방지 |\n| token/run 불일치 | project/group/doc/scope/owner 중 하나 불일치 | 403, 핸들러 미호출 | 모델 input으로 수정 불가 |\n| 그룹 worktree 없음 | run root 부재 또는 ledger 불일치 | read/write/test 모두 HTTP 409, `conflict`, reason `group_worktree_unavailable` | base checkout fallback 금지 |\n| 절대·상위 경로 | drive/UNC/`..`/symlink escape | 422 | 사전+서비스 이중 검증 |\n| 검색 glob escape | glob에 상위 경로 포함 | 422 | glob도 path-bearing 입력 |\n| mutation 중 merge conflict | document scope에서 열린 충돌 session | 409 | resolve_conflict 경로만 별도 처리 |\n| 테스트 미등록 명령 | exact allowlist 일치 없음 | 422 invalid_request, reason `not_verified` | 임의 shell 금지 |\n| 다른 OS 검증 명령 | verified_os가 현재 host와 다름 | 미등록과 동일 처리 | shell 차이 보호 |\n| 테스트 timeout | 서버가 정한 `min(test_timeout_default, run.remaining_seconds)` 도달 | process tree 종료 후 P0007 고정 결과 필드만 반환 | 모델 입력 timeout 및 `passed`/`timed_out` 필드 없음 |\n| 테스트 출력 과다 | 스트림 상한 초과 | tail 보존, truncated 표시 | 메모리·context 상한 |\n| run cancel/deadline | 실행 전 또는 중 발생 | 새 호출 금지, 실행 중 process tree 회수, run 종료 | background 잔존 금지 |\n| 내부 HTTP/DB 오류 | 하위 서비스 연결·조회 실패 | secret 없는 `unavailable` 결과 | stack 미노출 |\n| 질문 등록 성공 | pending 질문 생성 | 완료 도구를 재촉하지 않고 waiting 상태 | 기존 question_pending 사용 |\n| 나눗셈 | 이 로직에 비율 계산 없음 | 분모 0 조건 없음 | 해당 없음 |\n\n### 5.1 구현 테스트 실행 경계\n\n후속 구현은 테스트를 다음 순서와 범위로 동기 실행한다.\n\n1. 신규 단위 파일에서 registry/tier/choice를 검증한다: 4개 document scope의 register schema 변형, 비-T/TR 3도구, T/TR 8도구, 3개 exclusive scope 단일 강제 도구, unknown scope fail-closed.\n2. OpenAI·Anthropic 양쪽 adapter가 복수 tools 배열과 auto/forced choice를 동일하게 직렬화하고, 노출 목록의 임의 tool_call 이름을 파싱하는지 검증한다.\n3. dispatcher 표 기반 테스트를 수행한다: 8개 이름의 올바른 handler 매핑, 미노출/unknown/비객체/schema 위반 무부수효과, 중간 호출의 registered 불변, 완료 성공의 단일 증가, 복수 호출 중 실패/완료 short-circuit.\n4. 경로 보안 테스트를 수행한다: 정상 상대경로, absolute/drive/UNC/`..`, glob escape, symlink/reparse escape, 다른 group root, base checkout fallback 거부, read→patch→test가 같은 root를 받는지 검증한다.\n5. run_test 테스트를 수행한다: allowlist exact match, 공백 정규화, 추가 인자 거부, OS mismatch, cwd 고정, exit 0/비영, timeout/cancel process-tree kill, stdout/stderr 상한과 secret 비노출.\n6. 기존 회귀를 함께 실행한다: `server/tests/test_ai_invoke_0187.py`, `server/tests/test_ai_api_chat_turn_0489.py`, `server/tests/test_openai_api_base_url_contract_0489.py`, `server/tests/test_ai_invoke_worktree_gate_0299.py`, `server/tests/test_remote_tool_patch_stat_0347.py`, `server/tests/test_project_test_commands.py` 및 신규 0492 테스트 파일.\n7. 위 선택 스위트가 모두 통과한 뒤에만 `server/tests` 전체를 동기 실행한다. 장시간이라는 이유로 background 실행 후 턴을 끝내지 않는다. 환경 의존 실패는 동일 명령을 반복해 숨기지 않고 실패 test, 원인, 미실행 범위를 TR에 기록한다.\n\n테스트 자체도 저장소 source tree가 아니라 run의 그룹 worktree에서 실행한다. 테스트 fixture가 만드는 임시 디렉터리는 테스트 framework가 관리하되 제품 dispatcher의 cwd 결정에는 사용하지 않는다.\n\n## DEFERRED\n\n없음.\n\n## 체크리스트\n\n- [x] 모든 수치·집합 파라미터를 1절에 모았다.\n- [x] 단일 도구 구조를 목록 선택과 이름 기반 디스패치로 확장하는 순서를 확정했다.\n- [x] 경로 lexical/real-path 이중 검증과 group worktree 단일 root를 확정했다.\n- [x] run_test의 allowlist, cwd, timeout, 출력, process 회수 경계를 확정했다.\n- [x] 중간 도구와 완료 도구의 turn 전이 및 기본 거부 분기를 명시했다.\n- [x] 선택 회귀와 전체 테스트의 동기 실행 경계를 명시했다.\n- [x] 나눗셈이 없음을 경계 조건에 명시했다.\n\n---\n\nnext_type: L\nnext_type_detail: 로직\nproject: flowgate\nmodule: default\ngroup: 0492\ntitle: API 프로바이더 도구 호출 오류 정규화·완료 판정 로직설계\ntarget_id: B0001\n","doc_review_status":"approved","rejection_reason":"## Automated review rejection\n\n8개 도구·turn 상태전이 골격은 P0007/L0008/D0005와 대체로 정합합니다(파라미터, 이름 매핑, resolve_bound_step_type이 기대는 get_sequence_for_member_doc/get_effective_head 등은 실제 코드에도 존재). 다만 run_test 관련 두 곳(모델이 줄 수 없는 timeout_sec 참조, duration/passed/timed_out 필드명이 P0007 확정 예시와 불일치)과, 그룹 worktree 실패·미노출 도구·toolset 불가 상황에서 쓰는 새 식별자(group_worktree_unavailable/invalid_tool_call/toolset_unavailable)가 P0007이 이미 고정한 tool_result error.code 열거값 체계와의 관계를 명시하지 않은 점을 고쳐야 합니다.\n\n- §1 파라미터 정의(test_timeout_max) / §2.6 run_test 실행 경계: run_test 입력에 모델이 timeout을 지정할 수 있다고 서술합니다 — §1은 test_timeout_max를 '모델 입력으로 요청 가능한 테스트 상한'이라 정의하고, §2.6 의사코드는 'timeout = input.timeout_sec if present else test_timeout_default'로 input.timeout_sec을 읽습니다. 그러나 상위 확정 문서 P0007의 run_test tool_schema는 properties가 command 하나뿐이고 additionalProperties:false로 고정되어 있어 timeout_sec 필드 자체가 존재하지 않습니다(P0007 [run_test] 절). 모델이 보낼 수 없는 필드를 읽는 분기이므로 죽은 코드이거나, P0007과 어긋나는 스키마 확장입니다. timeout은 항상 test_timeout_default와 run 잔여 예산만으로 서버가 결정하도록 고치거나, P0007에 필드 추가가 필요함을 명시해야 합니다.\n- §2.6 run_test 실행 경계 — tool_result 반환 필드: 'return exit_code, timed_out, duration, truncated flags and bounded tails'와 이어지는 'ok=true, passed=false' 서술이 P0007의 run_test 확정 예시(S: tool_result = {ok, command, exit_code, duration_ms, stdout, stderr, truncated})와 필드 이름·존재 여부가 다릅니다. P0007에는 duration_ms인데 L0009는 duration으로 표기하고, P0007에는 없는 passed/timed_out을 새로 추가합니다. P0007이 이미 고정한 필드명(duration_ms 등)을 그대로 재사용하거나, 새 필드 추가를 P0007 개정 필요 사항으로 명시해야 합니다.\n- §2.4 run 고정 실행 컨텍스트 — 그룹 worktree 실패 코드: 'mutation과 run_test는 409 group_worktree_unavailable로 fail-closed한다'라고 되어 있는데, P0007의 [공통 오류 코드] 표는 HTTP 409를 항상 code=\"conflict\"로 정규화하고 원인은 error.details(예: patch_source_file의 worktree_missing/merge_conflict_open/provision_failed)로만 구분하도록 이미 고정했습니다. group_worktree_unavailable을 top-level code처럼 표기하면 provider가 받는 error.code 열거값(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)을 벗어납니다. 또한 §2.2/§2.3/§4에서 쓰는 invalid_tool_call, §2.1/§5의 toolset_unavailable도 이 tool_result error.code 열거값과의 관계(내부 상태 라벨인지, 아니면 code 값 그 자체인지)가 불명확합니다. 세 식별자 모두 P0007의 고정 code 열거값 중 무엇으로 정규화되는지, 그리고 세부 원인은 details의 어느 키(reason/cause)에 싣는지를 명시해야 합니다.\n\nGET http://127.0.0.1:8089/flowgate/api/v1/document/flowgate.default.0492.0009-L/reviews","rejection_history":[{"rejection_id":"rej_01M1CA07ERS87805","reason":"## Automated review rejection\n\n8개 도구·turn 상태전이 골격은 P0007/L0008/D0005와 대체로 정합합니다(파라미터, 이름 매핑, resolve_bound_step_type이 기대는 get_sequence_for_member_doc/get_effective_head 등은 실제 코드에도 존재). 다만 run_test 관련 두 곳(모델이 줄 수 없는 timeout_sec 참조, duration/passed/timed_out 필드명이 P0007 확정 예시와 불일치)과, 그룹 worktree 실패·미노출 도구·toolset 불가 상황에서 쓰는 새 식별자(group_worktree_unavailable/invalid_tool_call/toolset_unavailable)가 P0007이 이미 고정한 tool_result error.code 열거값 체계와의 관계를 명시하지 않은 점을 고쳐야 합니다.\n\n- §1 파라미터 정의(test_timeout_max) / §2.6 run_test 실행 경계: run_test 입력에 모델이 timeout을 지정할 수 있다고 서술합니다 — §1은 test_timeout_max를 '모델 입력으로 요청 가능한 테스트 상한'이라 정의하고, §2.6 의사코드는 'timeout = input.timeout_sec if present else test_timeout_default'로 input.timeout_sec을 읽습니다. 그러나 상위 확정 문서 P0007의 run_test tool_schema는 properties가 command 하나뿐이고 additionalProperties:false로 고정되어 있어 timeout_sec 필드 자체가 존재하지 않습니다(P0007 [run_test] 절). 모델이 보낼 수 없는 필드를 읽는 분기이므로 죽은 코드이거나, P0007과 어긋나는 스키마 확장입니다. timeout은 항상 test_timeout_default와 run 잔여 예산만으로 서버가 결정하도록 고치거나, P0007에 필드 추가가 필요함을 명시해야 합니다.\n- §2.6 run_test 실행 경계 — tool_result 반환 필드: 'return exit_code, timed_out, duration, truncated flags and bounded tails'와 이어지는 'ok=true, passed=false' 서술이 P0007의 run_test 확정 예시(S: tool_result = {ok, command, exit_code, duration_ms, stdout, stderr, truncated})와 필드 이름·존재 여부가 다릅니다. P0007에는 duration_ms인데 L0009는 duration으로 표기하고, P0007에는 없는 passed/timed_out을 새로 추가합니다. P0007이 이미 고정한 필드명(duration_ms 등)을 그대로 재사용하거나, 새 필드 추가를 P0007 개정 필요 사항으로 명시해야 합니다.\n- §2.4 run 고정 실행 컨텍스트 — 그룹 worktree 실패 코드: 'mutation과 run_test는 409 group_worktree_unavailable로 fail-closed한다'라고 되어 있는데, P0007의 [공통 오류 코드] 표는 HTTP 409를 항상 code=\"conflict\"로 정규화하고 원인은 error.details(예: patch_source_file의 worktree_missing/merge_conflict_open/provision_failed)로만 구분하도록 이미 고정했습니다. group_worktree_unavailable을 top-level code처럼 표기하면 provider가 받는 error.code 열거값(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)을 벗어납니다. 또한 §2.2/§2.3/§4에서 쓰는 invalid_tool_call, §2.1/§5의 toolset_unavailable도 이 tool_result error.code 열거값과의 관계(내부 상태 라벨인지, 아니면 code 값 그 자체인지)가 불명확합니다. 세 식별자 모두 P0007의 고정 code 열거값 중 무엇으로 정규화되는지, 그리고 세부 원인은 details의 어느 키(reason/cause)에 싣는지를 명시해야 합니다.\n\nGET http://127.0.0.1:8089/flowgate/api/v1/document/flowgate.default.0492.0009-L/reviews","rejected_at":"2026-09-01T01:21:23+09:00","rejected_by":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","ai_response":"① run_test 입력은 P0007의 command 단일 스키마를 유지하도록 timeout_sec 분기와 test_timeout_max를 제거하고, 서버가 test_timeout_default와 run 잔여 예산의 최솟값으로 제한하도록 수정했습니다. ② run_test 결과를 P0007 고정 필드 {ok, command, exit_code, duration_ms, stdout, stderr, truncated}로 통일하고 duration/passed/timed_out/result_truncated 표기를 제거했습니다. ③ group_worktree_unavailable, invalid_tool_call, toolset_unavailable을 top-level code가 아닌 내부 원인 라벨로 명시하고 각각 conflict/invalid_request/unavailable로 정규화하며 error.details.reason에 싣도록 본문·의사코드·결정 트리·예외 표를 일관되게 수정했습니다.","responded_at":"2026-09-01T01:28:41+09:00","response_recorded_by":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","response_revision_no":1,"review_id":503}],"created_at":"2026-09-01T01:14:53+09:00","updated_at":"2026-09-01T01:28:53+09:00","ai_review":{"id":503,"revision_no":0,"reviewer_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","verdict":"issues","finding_count":3,"findings":[{"locus":"§1 파라미터 정의(test_timeout_max) / §2.6 run_test 실행 경계","note":"run_test 입력에 모델이 timeout을 지정할 수 있다고 서술합니다 — §1은 test_timeout_max를 '모델 입력으로 요청 가능한 테스트 상한'이라 정의하고, §2.6 의사코드는 'timeout = input.timeout_sec if present else test_timeout_default'로 input.timeout_sec을 읽습니다. 그러나 상위 확정 문서 P0007의 run_test tool_schema는 properties가 command 하나뿐이고 additionalProperties:false로 고정되어 있어 timeout_sec 필드 자체가 존재하지 않습니다(P0007 [run_test] 절). 모델이 보낼 수 없는 필드를 읽는 분기이므로 죽은 코드이거나, P0007과 어긋나는 스키마 확장입니다. timeout은 항상 test_timeout_default와 run 잔여 예산만으로 서버가 결정하도록 고치거나, P0007에 필드 추가가 필요함을 명시해야 합니다."},{"locus":"§2.6 run_test 실행 경계 — tool_result 반환 필드","note":"'return exit_code, timed_out, duration, truncated flags and bounded tails'와 이어지는 'ok=true, passed=false' 서술이 P0007의 run_test 확정 예시(S: tool_result = {ok, command, exit_code, duration_ms, stdout, stderr, truncated})와 필드 이름·존재 여부가 다릅니다. P0007에는 duration_ms인데 L0009는 duration으로 표기하고, P0007에는 없는 passed/timed_out을 새로 추가합니다. P0007이 이미 고정한 필드명(duration_ms 등)을 그대로 재사용하거나, 새 필드 추가를 P0007 개정 필요 사항으로 명시해야 합니다."},{"locus":"§2.4 run 고정 실행 컨텍스트 — 그룹 worktree 실패 코드","note":"'mutation과 run_test는 409 group_worktree_unavailable로 fail-closed한다'라고 되어 있는데, P0007의 [공통 오류 코드] 표는 HTTP 409를 항상 code=\"conflict\"로 정규화하고 원인은 error.details(예: patch_source_file의 worktree_missing/merge_conflict_open/provision_failed)로만 구분하도록 이미 고정했습니다. group_worktree_unavailable을 top-level code처럼 표기하면 provider가 받는 error.code 열거값(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)을 벗어납니다. 또한 §2.2/§2.3/§4에서 쓰는 invalid_tool_call, §2.1/§5의 toolset_unavailable도 이 tool_result error.code 열거값과의 관계(내부 상태 라벨인지, 아니면 code 값 그 자체인지)가 불명확합니다. 세 식별자 모두 P0007의 고정 code 열거값 중 무엇으로 정규화되는지, 그리고 세부 원인은 details의 어느 키(reason/cause)에 싣는지를 명시해야 합니다."}],"comment":"8개 도구·turn 상태전이 골격은 P0007/L0008/D0005와 대체로 정합합니다(파라미터, 이름 매핑, resolve_bound_step_type이 기대는 get_sequence_for_member_doc/get_effective_head 등은 실제 코드에도 존재). 다만 run_test 관련 두 곳(모델이 줄 수 없는 timeout_sec 참조, duration/passed/timed_out 필드명이 P0007 확정 예시와 불일치)과, 그룹 worktree 실패·미노출 도구·toolset 불가 상황에서 쓰는 새 식별자(group_worktree_unavailable/invalid_tool_call/toolset_unavailable)가 P0007이 이미 고정한 tool_result error.code 열거값 체계와의 관계를 명시하지 않은 점을 고쳐야 합니다.","reviewed_at":"2026-09-01T01:21:13+09:00","created_at":"2026-09-01T01:21:13+09:00"},"ai_review_history":[{"id":503,"revision_no":0,"reviewer_id":"4d96c7c2-c0be-4f4e-8594-bd65d2a8fa39","verdict":"issues","finding_count":3,"findings":[{"locus":"§1 파라미터 정의(test_timeout_max) / §2.6 run_test 실행 경계","note":"run_test 입력에 모델이 timeout을 지정할 수 있다고 서술합니다 — §1은 test_timeout_max를 '모델 입력으로 요청 가능한 테스트 상한'이라 정의하고, §2.6 의사코드는 'timeout = input.timeout_sec if present else test_timeout_default'로 input.timeout_sec을 읽습니다. 그러나 상위 확정 문서 P0007의 run_test tool_schema는 properties가 command 하나뿐이고 additionalProperties:false로 고정되어 있어 timeout_sec 필드 자체가 존재하지 않습니다(P0007 [run_test] 절). 모델이 보낼 수 없는 필드를 읽는 분기이므로 죽은 코드이거나, P0007과 어긋나는 스키마 확장입니다. timeout은 항상 test_timeout_default와 run 잔여 예산만으로 서버가 결정하도록 고치거나, P0007에 필드 추가가 필요함을 명시해야 합니다."},{"locus":"§2.6 run_test 실행 경계 — tool_result 반환 필드","note":"'return exit_code, timed_out, duration, truncated flags and bounded tails'와 이어지는 'ok=true, passed=false' 서술이 P0007의 run_test 확정 예시(S: tool_result = {ok, command, exit_code, duration_ms, stdout, stderr, truncated})와 필드 이름·존재 여부가 다릅니다. P0007에는 duration_ms인데 L0009는 duration으로 표기하고, P0007에는 없는 passed/timed_out을 새로 추가합니다. P0007이 이미 고정한 필드명(duration_ms 등)을 그대로 재사용하거나, 새 필드 추가를 P0007 개정 필요 사항으로 명시해야 합니다."},{"locus":"§2.4 run 고정 실행 컨텍스트 — 그룹 worktree 실패 코드","note":"'mutation과 run_test는 409 group_worktree_unavailable로 fail-closed한다'라고 되어 있는데, P0007의 [공통 오류 코드] 표는 HTTP 409를 항상 code=\"conflict\"로 정규화하고 원인은 error.details(예: patch_source_file의 worktree_missing/merge_conflict_open/provision_failed)로만 구분하도록 이미 고정했습니다. group_worktree_unavailable을 top-level code처럼 표기하면 provider가 받는 error.code 열거값(invalid_request/unauthorized/forbidden/not_found/conflict/too_large/unavailable)을 벗어납니다. 또한 §2.2/§2.3/§4에서 쓰는 invalid_tool_call, §2.1/§5의 toolset_unavailable도 이 tool_result error.code 열거값과의 관계(내부 상태 라벨인지, 아니면 code 값 그 자체인지)가 불명확합니다. 세 식별자 모두 P0007의 고정 code 열거값 중 무엇으로 정규화되는지, 그리고 세부 원인은 details의 어느 키(reason/cause)에 싣는지를 명시해야 합니다."}],"comment":"8개 도구·turn 상태전이 골격은 P0007/L0008/D0005와 대체로 정합합니다(파라미터, 이름 매핑, resolve_bound_step_type이 기대는 get_sequence_for_member_doc/get_effective_head 등은 실제 코드에도 존재). 다만 run_test 관련 두 곳(모델이 줄 수 없는 timeout_sec 참조, duration/passed/timed_out 필드명이 P0007 확정 예시와 불일치)과, 그룹 worktree 실패·미노출 도구·toolset 불가 상황에서 쓰는 새 식별자(group_worktree_unavailable/invalid_tool_call/toolset_unavailable)가 P0007이 이미 고정한 tool_result error.code 열거값 체계와의 관계를 명시하지 않은 점을 고쳐야 합니다.","reviewed_at":"2026-09-01T01:21:13+09:00","created_at":"2026-09-01T01:21:13+09:00"}],"test_run":null,"test_run_history":[]}